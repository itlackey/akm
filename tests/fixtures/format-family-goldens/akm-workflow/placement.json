{
  "scenario": "akm-workflow adapter placeNew(component, conceptId) → on-disk relative path. Mirrors placement/all-types.json's byType + workflow-probe edge cases. SPECIFICATION golden.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "NOT IMPLEMENTED — spec-authored target (future Chunk-2 format-adapter work item).",
  "specSource": "docs/architecture/specs/akm-0.9.0-bundle-adapter-spec.md §2 (placeNew), §6 (workflow row).",
  "realWorldSource": [
    "AKM-native; mirrors the frozen placement/all-types.json workflow edge cases (explicit-extension short-circuit, .md default, real YAML-only candidate)"
  ],
  "notes": [
    "SPECIFICATION golden. Component-root-relative POSIX paths. The workflow placement default is <conceptId>.md; an explicit .yaml/.yml conceptId short-circuits to that extension (mirrors the frozen all-types placement golden's workflow multi-extension probe branches)."
  ],
  "byType": {
    "workflow": {
      "conceptId": "onboarding",
      "assetPath": "onboarding.md",
      "name": "onboarding",
      "note": "default markdown placement when no explicit workflow-program extension is given."
    }
  },
  "edgeCases": {
    "explicitYamlExtensionSkipsProbe": {
      "conceptId": "deploy.yaml",
      "assetPath": "deploy.yaml",
      "note": "an explicit .yaml conceptId short-circuits the extension probe (mirrors all-types placement 'workflowExplicitExtensionSkipsProbe')."
    },
    "defaultsToMdWhenNoExtension": {
      "conceptId": "release",
      "assetPath": "release.md",
      "note": "no extension → .md default (mirrors all-types 'workflowProbeFallbackToMdWhenNoCandidateExists')."
    }
  }
}
