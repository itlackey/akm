{
  "scenario": "presentationFor(type) for generic-files types: `script` has a presentation entry (script-source); `document` and `file` are NOT in KNOWN_TYPES → generic fallback. Mirrors renderer/all-types.json's byRelPath shape. SPECIFICATION golden.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "NOT IMPLEMENTED — spec-authored target (future Chunk-2 format-adapter work item).",
  "specSource": "docs/design/akm-0.9.0-bundle-adapter-spec.md §2 / §3.4; src/core/type-presentation.ts (TYPE_PRESENTATION['script'] = script-source); src/core/recognition-util.ts (KNOWN_TYPES includes 'script' but NOT 'document' or 'file').",
  "realWorldSource": [
    "AKM-native fallback family"
  ],
  "notes": [
    "SPECIFICATION golden. `script` IS a known type → renderer 'script-source', action `akm show <ref> -> execute the run command`. `document` and `file` are NOT in KNOWN_TYPES → generic fallback {label:'Asset'}, renderer=null, action `akm show <ref>` (spec §3.4).",
    "RESOLVED (open question 4, maintainer resolution 2026-07): `document` and `file` get NO dedicated presentation entries — the generic fallback is CONFIRMED (in contrast to `instruction`, which DID get its own entry under open question 1). `script` keeps its type-specific presentation (script-source). No longer open."
  ],
  "byRelPath": {
    "build.sh": {
      "type": "script",
      "ref": "sample-generic//build.sh",
      "label": "Script",
      "renderer": "script-source",
      "action": "akm show sample-generic//build.sh -> execute the run command"
    },
    "notes.md": {
      "type": "document",
      "ref": "sample-generic//notes",
      "label": "Asset",
      "renderer": null,
      "action": "akm show sample-generic//notes",
      "flag": "GENERIC FALLBACK — `document` has no TYPE_PRESENTATION entry."
    },
    "data.csv": {
      "type": "file",
      "ref": "sample-generic//data.csv",
      "label": "Asset",
      "renderer": null,
      "action": "akm show sample-generic//data.csv",
      "flag": "GENERIC FALLBACK — `file` has no TYPE_PRESENTATION entry."
    }
  }
}
