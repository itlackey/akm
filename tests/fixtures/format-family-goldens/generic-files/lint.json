{
  "scenario": "generic-files adapter validate → base checks only (no type-specific validators). Mirrors lint/all-types.json's perType shape. SPECIFICATION golden.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "NOT IMPLEMENTED — spec-authored target (future Chunk-2 format-adapter work item).",
  "specSource": "docs/design/akm-0.9.0-bundle-adapter-spec.md §7 (generic-files row), §6 (script row: 'none' type-specific validation; document/file have no validators). Diagnostic {file, issue, detail, fixed}.",
  "realWorldSource": [
    "AKM-native fallback family — no native validation beyond base checks"
  ],
  "notes": [
    "SPECIFICATION golden. generic-files has NO type-specific validators (script/document/file each fall through to base checks), so every well-formed file → []. This mirrors the frozen all-types lint golden, where script/env(bare)/secret/wiki/session dispatch to the shared DefaultLinter."
  ],
  "perType": {
    "document": {
      "relPath": "notes.md",
      "type": "document",
      "issues": [],
      "note": "base checks only."
    },
    "script": {
      "relPath": "build.sh",
      "type": "script",
      "issues": [],
      "note": "script type has no dedicated validator (spec §6 script row: 'none') → base checks only → []."
    },
    "file": {
      "relPath": "data.csv",
      "type": "file",
      "issues": [],
      "note": "arbitrary file → base checks only."
    }
  }
}
