{
  "scenario": "akm-task adapter placeNew(component, conceptId) → on-disk relative path. Mirrors placement/all-types.json's byType shape. SPECIFICATION golden.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "NOT IMPLEMENTED — spec-authored target (future Chunk-2 format-adapter work item).",
  "specSource": "docs/design/akm-0.9.0-bundle-adapter-spec.md §2 (placeNew), §6 (task row: .yml).",
  "realWorldSource": [
    "AKM-native; mirrors the frozen placement/all-types.json task entry (tasks/<name>.yml)"
  ],
  "notes": [
    "SPECIFICATION golden. Component-root-relative POSIX path. A task places to <conceptId>.yml.",
    "edgeCase pins the already-suffixed-name idempotence branch the frozen all-types placement golden documents (taskAlreadySuffixedNameIsIdempotent)."
  ],
  "byType": {
    "task": {
      "conceptId": "weekly-digest",
      "assetPath": "weekly-digest.yml",
      "name": "weekly-digest"
    }
  },
  "edgeCases": {
    "taskAlreadySuffixedNameIsIdempotent": {
      "conceptId": "nightly-index.yml",
      "assetPath": "nightly-index.yml",
      "note": "a conceptId already ending in .yml does not gain a second suffix (mirrors all-types 'taskAlreadySuffixedNameIsIdempotent')."
    }
  }
}
