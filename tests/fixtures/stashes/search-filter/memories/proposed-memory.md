---
description: Gridlock guidance marked proposed quality, scoped to a single user.
tags:
  - gridlock
  - proposed
quality: proposed
scope_user: alice
---

# Proposed Memory

Gridlock guidance still awaiting review (`quality: proposed`). Excluded from
default search on both paths (`isProposedQuality`) unless the caller opts in
via `includeProposed`. Carries no `beliefState` of its own.
