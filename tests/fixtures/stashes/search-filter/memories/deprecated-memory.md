---
description: Gridlock guidance carrying the deprecated belief state, scoped to a single channel.
tags:
  - gridlock
  - deprecated
quality: generated
beliefState: deprecated
scope_channel: cli
---

# Deprecated Memory

Gridlock guidance marked deprecated. Scoped to the `cli` channel so it
exercises `entryMatchesScope`'s `channel` key alongside the `historical`
belief filter (`deprecated` counts as historical per Phase 1A).
