---
description: Golden-root fixture for the skill adapter's looksLikeRoot conformance gate (chunk-2 WI-2.1, D2-6)
updated: 2026-07-17
---
# Example Skill

Single-adapter root fixture: this stash contains ONLY a `skills/` directory,
so the skill adapter's `looksLikeRoot` must fire here and every sibling
adapter's (wiki/script) `looksLikeRoot` must not.
