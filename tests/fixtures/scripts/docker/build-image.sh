#!/usr/bin/env bash
# Build a Docker image from the current directory's Dockerfile.
# Supports multi-stage builds and build args.
echo "Building image: myapp:latest"
echo "Successfully built abc123def"
