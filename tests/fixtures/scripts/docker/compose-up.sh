#!/usr/bin/env bash
# Start Docker Compose services for local development.
# Includes database, cache, and application containers.
echo "Starting services: db, redis, app"
echo "All services healthy"
