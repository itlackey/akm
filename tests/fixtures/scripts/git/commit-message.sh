#!/usr/bin/env bash
# Generate a conventional commit message from staged changes.
# Analyzes the diff and produces a commit message following conventional commits spec.
echo "feat(auth): add JWT token refresh middleware"
