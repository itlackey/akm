# Task: configure CPU-based autoscaling

Edit `service.yaml` to configure CPU-based autoscaling.

Requirements:
- minimum 1 replica
- maximum 8 replicas
- scale based on CPU utilization
- target: 65 percent CPU

Consult the inkwell skill for the correct field names and metric value.
