# RFC 9111 — HTTP Caching (excerpt)

An immutable ingested source. Never edited. Pages cite this file in their
`sources:` frontmatter.

A cache stores responses and reuses them to reduce latency and load. Freshness
is governed by `Cache-Control` and `Expires`.
