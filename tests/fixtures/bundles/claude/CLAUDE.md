# Sample project instructions

Project-level guidance Claude Code loads into context at the start of every
session. This fixture stands in for a real `CLAUDE.md`: it carries conventions,
common commands, and architectural notes — no YAML frontmatter, because a
`CLAUDE.md` is a plain instruction file.

## Conventions

- Run `bun test` before every commit.
- Keep functions small and pure where possible.
