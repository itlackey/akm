# PDF form-filling reference

A bundled Level-3 resource for the `pdf-processing` skill. Claude reads this
file only when the SKILL.md instructions point at it. It is part of the skill
item (the directory), not a standalone concept.
