# Findings

Review outputs land here, one file per prompt (`NN-slug.md`).

**Findings are NOT committed.** They analyze the owner's live install and can
contain security-sensitive facts (what is exposed, and where). The directory
is gitignored except this README; durable copies live in the owner's private
session memories. Keep working copies here so later prompts can reference
earlier findings.
