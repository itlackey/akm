# Chunk 7 Implementation Brief — improve decomposition + dead-lane deletions

Branch: `akm-090/chunk-7` (cut from `akm-090/wave-1` @ 8f7139b) · Wave 1, order 2 · Net-LOC target (reported, never gated): ~−3,900 improve/memory/salience + ~−1,300 recombine fold-in (§12.1 residual row)

*Revision 2 — incorporates the adversarial verification panel's findings: D6 memo scope/invalidation made co-satisfiable and test-pinned; migration-018 raw-SQL census completed (mv.test.ts, monitor-liveness, outcome-invariance); processSession arithmetic corrected to 18; ws4-gate-coherence anchored by describe name; consumer censuses completed for recombine (standards-prompt-injection), high-retrieval (health-minting-rollup, distill.test.ts), minRetrievalCount (12 files), review_pressure type field (monitor-liveness, outcome-loop), and judged-cache/hotProbation (improve-target-integration, improve-strategies).*

**Authority.** `docs/design/akm-0.9.0-bundle-adapter-architecture-plan.md` is THE plan. Nothing in current code supersedes it. Where the plan says *preserve behavior*, current behavior is the oracle; everywhere else the plan is. All line anchors below were re-measured at 8f7139b and win over the plan's stale anchors.

**Hard rules (plan §1.3 — violations are review BLOCKERS).**
- NO new trust/approval/security machinery: no labeling, no action clamps, no confirm prompts, no digests, no trust records. (R33)
- Memory lifecycle is DEFERRED entirely (plan §6): no operational states, no water-marks, no pressure computation, no intake blocking, no CAS archive, no sandbox gate, no purge/quarantine commands. Anything lifecycle-shaped is out of scope BY DESIGN — do not "helpfully" add it. (R32)
- Deletion is gated by inventory + zero-count greps, never by a LOC number; net-LOC is reported, not gated. (R41)
- §15.3 safety suites stay green at every item boundary. Fixed points — `tests/_helpers/sandbox.ts`, `tests/_preload.ts`, the mock.module-ban lint (`scripts/lint-tests-isolation.ts` Rule 6 + `ALLOWLIST_RATCHET_BASELINE`), the hand-rolled sharding (`scripts/run-test-shard.sh`, `scripts/test-unit.sh`) — are UNTOUCHED. (R40)

**Operational traps (read before running anything).**
- `bun test <paths>` MUST carry `--timeout=30000` (bun 1.3.11 ignores bunfig `[test]` timeout; the 0a golden suites run 12–18s and fail spuriously at the 5s default, then trip the `[sandbox tripwire] leaked env vars` error). Or go through `scripts/test-unit.sh` / `scripts/run-test-shard.sh`.
- Never run two `bun test` invocations concurrently in the same workspace (produces hundreds of false failures).
- Golden fixtures regenerate only under `AKM_UPDATE_GOLDENS=1` (`tests/_helpers/golden.ts:175`). Every fixture is registered in `tests/fixtures/goldens/DESIGNATIONS.json`, policed by `tests/goldens-designations.test.ts` — registry entries must be edited in the SAME commit as any fixture/consumer deletion, in both directions.
- Code comments in this area lie: `preparation.ts:1342` ("DEFAULT OFF" — proactive is default-ON), `preparation.ts:426`/`config-schema.ts:347` ("Default OFF" — judgedCache is default-ON). Gate every deletion on code, not comments.

---

## 1. Binding decisions (made by this brief; grounded; reviewers hold the dev to these)

- **D1 (R23) — `getNeighborsByEntryId` is KEPT.** Plan §14.3's "recombine-orphaned" premise is half wrong: `src/commands/improve/consolidate.ts:59,:2859` (`narrowToIncrementalCandidates`, the `incrementalSince` incremental-consolidation path, pinned by `tests/commands/consolidate/consolidate-incremental.test.ts`) is a live consumer of preserved consolidate behavior (R31). R23 is scoped to `getEntitiesByEntryIds` (`src/indexer/db/db.ts:1109`, genuinely recombine-only). `getNeighborsByEntryId` goes in the deletion ledger as retained-with-reason.
- **D2 (R17) — all six branches are deleted per the plan's DELETE ruling, but two are reclassified as live behavior changes**: judged-state cache #581 is DEFAULT ON at HEAD (`consolidate.ts:1282` `!== false`) and schemaSimilarity is DEFAULT ON since R3 (`extract.ts:1159` `!== false`, inert without derived embeddings). Their deletion changes default behavior (memories re-judged every consolidate run → more LLM calls; no schema-consistency confidence penalty at extract). Both get §5 behavior-change-ledger entries and are reviewed as live changes (DoD 6) — not silently deleted as "dead".
- **D3 (R13) — the health calibration surface dies in this chunk.** `calibration.ts` deletion cascades: `src/commands/health/types.ts:12,:216` (non-optional `calibration: CalibrationSummary`), `health/metrics.ts:16,:53-56` (`readCalibration`), `health/improve-metrics.ts:16,:69`, `health/html-report.ts:749-770`, `health.ts:36,:213`, `health/windows.ts:26,:231`. Wave order is 7→6, so Chunk 7 cannot defer to Chunk 6 (which deletes the data source, `recordGateDecision`). Migration 012's `improve_gate_thresholds` store and `makeGateConfig`'s read side are Chunk 6 scope — after this chunk nothing writes the table; the read path is left inert but intact.
- **D4 (R14) — the extract-side orderedActions/outcomeData capture is deleted with the procedural lane** (`extract-prompt.ts:93-103,:223-229,:294-302`; `extract.ts:474-479,:842-845`). Sole reader is `procedural.ts`. This changes default extract prompts/frontmatter → §5 ledger entry. Alternative (keep write-only frontmatter) rejected: violates the dead-code inventory.
- **D5 (R18/R22/R17c schema) — one appended migration, historical entries untouched.** Migrations are append-only (`migrations.ts:5-9`); entries 007/010/014 are never edited. WI-7.3 appends `018-drop-dead-lane-schema`: `DROP INDEX idx_recombine_hypotheses_*` + `DROP TABLE recombine_hypotheses`; `DROP TABLE consolidation_judged`; `DROP INDEX idx_asset_outcome_review_pressure` + `ALTER TABLE asset_outcome DROP COLUMN review_pressure`. The `sqlite-migrations.characterization.test.ts` DDL snapshot is re-recorded once, deliberately, in the same commit. Full `review_pressure` test census (repo-wide grep, complete — deletion is gated by inventory): raw INSERT column lists in `tests/integration/mv-durable-recovery.test.ts:116,:166,:286`, `tests/integration/goldens-mv-recovery.test.ts:134`, `tests/commands/mv.test.ts:1229,:1236,:1243`, and `tests/commands/improve/outcome-invariance.test.ts:49` (all: drop the column from the list — fixture bytes unchanged); `tests/commands/mv.test.ts:1278-1284` SELECTs and asserts review_pressure VALUES (3/2/4) — this is the outcome-carry-on-rename contract and needs semantic rework, not a line delete: keep the `retrieval_count`/`outcome_score` carry assertions as the surviving pin, drop review_pressure from the SELECT, the row type, and the expected objects; `tests/commands/improve/monitor-liveness.test.ts:35` (typed `AssetOutcomeRow` builders) and `tests/commands/improve/outcome-loop.test.ts` break at TSC on the FIELD deletion and are therefore updated in WI-7.2, not here. Migration-ID-pinning tests (`migration-backup.test.ts:350`, `migration-lifecycle-regression.test.ts:53`, `integration/migration-apply-crash.test.ts:35`, `storage/engine-cutover-historical-migrations.test.ts:38`) keep passing because no existing ID moves.
- **D6 (R9) — read-once seam with pass-scoped memoization + write-through invalidation; the hash-manifest defers to Chunk 6.** The plan's "hash-manifest snapshot" mechanism has zero hits in src at HEAD, and Chunk 6 (which runs AFTER 7) owns `beforeHash`/`FileChange[]`. Chunk 7 mints `RunContext.readAsset` — a read-once seam whose memo is **NEVER run-wide**. Scope rules, binding: (i) prep's six stage-body passes share ONE memo instance, created only AFTER the mutating pre-loop passes have returned (`runConsolidationPass`'s op writes are on disk before seeding) and discarded at stage end; (ii) reflect and distill each get a FRESH memo per verb invocation, so their reads still happen at invoke time — this is what "read timing unchanged" means, and it matters because mid-run writes are real: the loop's accepted proposals mutate files between verb invocations, and `distill.ts:816` stamps salience frontmatter onto the source asset in-loop. (iii) Belt-and-braces: `RunContext` pairs `readAsset` with a write-invalidation hook (`noteAssetWrite(path)` or a `writeAsset` wrapper) that drops/refreshes the memo entry for a written path; every file-write site inside a live memo scope calls it (`distill.ts:816` is the known verb-internal one; consolidate op writes and maintenance-pass writes sit outside any live memo scope by rule (i)/(ii) but call it anyway if a scope is active). Consequence, pinned by unit test (WI-7.4): `readAsset` after an in-run write of the same path returns POST-write bytes, and reflect/distill reads observably happen at invoke time, not prep time. No content hashing is minted here.
- **D7 (R30) — FINISH the structured-call migration** (the plan's preferred branch, now strictly cheaper: 3 adopters at HEAD — `metadata-enhance.ts:63`, `graph-extract.ts:866`, `memory-infer-impl.ts:117` — not 1, and every chunk-7 raw caller already has a `chat` seam matching `CallStructuredRequest.chat`). Migrate exactly the chunk-7 files: `reflect.ts:904,:1483`, `distill.ts:740,:992` (WI-7.5); `extract.ts:1010,:1118` (WI-7.7); `consolidate.ts:1660,:3013`, `shared.ts:71`, `memory/memory-contradiction-detect.ts:222` (WI-7.8). Out-of-chunk raw callers (graph-extract retry path :660/:683, tasks/runner, workflows, sources/schema-repair) are flagged for Chunk 9, left raw. `_setChatCompletionForTests` (`llm/client.ts:274`) must keep working through `structured-call.ts:124`'s late binding — verify with existing seam tests.
- **D8 (R27) — partially descoped, recorded as mis-grounded.** `memory-improve.ts:728-735` is a read-only collector (no write — nothing to convert). `:313-333` mutates the BODY (`resolveRelativeDates`), which `mutateFrontmatter`'s contract (`core/asset/frontmatter.ts:150-164`, frontmatter-only, content preserved) cannot express. Decision: leave both as-is; ledger records the −20 row as not executable as specced. Do NOT extend `core/asset/frontmatter.ts` (wide blast radius; the frozen `improve/resolve-relative-dates.json` golden pins block 1's bytes).
- **D9 (R6) — the god-fn gate covers ALL of `src/commands/improve/**`,** including arrow/anonymous functions (the 389-LOC anon at `loop-stages.ts:1009` is real code), enforcement bar **220 lines** (= "~200" with tolerance). Twenty functions exceed it at HEAD, not the plan's 4-5. Mechanism: a TS-AST ratchet meta-test (WI-7.4) with a shrink-only exported baseline (pattern: `tests/lint-isolation-ratchet.test.ts` + `tests/architecture/agent-runner-seam.test.ts`), emptied by WI-7.8.
- **D10 (R8) — the envelope facade wraps the CURRENT Proposal shape** (`repository.ts:287` Proposal, `:198` `payload: {content, frontmatter?}`, status `pending|…`). No `FileChange[]`, no `beforeHash` — those land in Chunk 6. The facade is one module the five surviving emit sites adopt (`reflect.ts:1583`, `distill.ts:1231`, `distill/promote-memory.ts:248`, `extract.ts:825`, `consolidate.ts:2668`; the SC winner-persist `loop-stages.ts:337`, `recombine.ts:892`, `procedural.ts:453` die in WI-7.1/7.2).
- **D11 (R22/R14) — `'recombine'` and `'procedural'` are removed from `PROPOSAL_SOURCES`/`AUTOMATED_PROPOSAL_SOURCES`** (`proposal/repository.ts:101-102,:121-122`) and the PERSONAL_STASH recombine drain rule (`drain-policies.ts:56-61`) is deleted. Legacy pending proposals with those sources become unmatched and are swept by the existing 90-day TTL. Ledger note; no compatibility shim.
- **D12 (R16) — the four #616 stage seams are KEPT** (`improve.ts:131-137`: `collectEligibleRefsFn`/`runImprovePreparationStageFn`/`runImproveLoopStageFn`/`runImprovePostLoopStageFn`) — they are the DI that keeps `akmImprove` testable and WI-7.7 builds on them. Deleted: the cycle for-loop `:914-1135`, `maxCycles` option `:123-130` + resolution `:474` + config knob `config-schema.ts:653`, per-cycle accumulators `:887-912`, fixed-point break `:1130-1135`, inter-cycle git bank `:925-935` (#662 collapses to the single end-of-run commit), `runIndexAndCollect` re-run `:939-951`, `cyclesRun>1` reporting `:1231-1233`, multi-cycle comments (`:1504`).
- **D13 (R15/R35) — ledger entry (b) uses the corrected wording.** The plan's "P0-A is today the ONLY path for never-rated assets" is FALSE at HEAD: proactive-maintenance ships `enabled:true, maxPerRun:15` in the default strategy and the #608 high-salience lane also rescues never-rated refs (per-lane attribution pinned by `p0a-selection.json`: `{high-retrieval:1, high-salience:1, proactive:15, signal-delta:1}`). Post-deletion, never-rated assets remain reachable via proactive + high-salience; the delta is smaller than the plan claims and diff review must attribute per lane.
- **D14 (R28) — no db handle into `ProposalsContext`/`createProposal` in this chunk.** `withProposalsDb` (`repository.ts:510`) keeps its per-call open; threading the run handle risks the two-writers-on-one-WAL failure class (#585) and is explicitly Chunk 9 work. Chunk 7's R28 scope: the reflect/distill subtree (14 no-ctx sites), `locks.ts:45`, and extract's 3 sites (`:765,:869,:1355`, currently passed a dbPath-only ProposalsContext).
- **D15 (R42) — `improve/self-consistency.json` and `improve/p0a-selection.json` are RETIRED with their lanes** (the designated path per their re-baseline@7 notes): suite + fixture + DESIGNATIONS.json entry removed in the same commit (WI-7.2). All consolidate-surface fixtures + `improve/signal-delta-gate.json` + the two duration-flag fixtures are frozen-migration-input — zero diffs allowed all chunk.
- **D16 (R20) — Chunk 7 deletes both config subtrees** (`ImproveCalibrationSchema` `config-schema.ts:817-836`, `ImproveExplorationSchema` `:839-853`, registrations `:906-907`): §5 binds the schema halves to the lane deletion and both sole readers die in this chunk. Verify the parent schema's passthrough leniency keeps existing user configs with orphaned `improve.calibration`/`improve.exploration` blocks loading (`config-process-roundtrip.test.ts:357` behavior). Note the overlap in the chunk report for Chunk 9's config pass.
- **D17 (R7) — three-verb naming is vocabulary ONLY**: revise = reflect; learn = extract/distill/inference (recombine/synthesis removed from "learn" by §13.1); consolidate = the existing memory-tier ops. It lands as module/pass naming, doc comments, and the facade's documentation. Zero new machinery, zero renamed CLI surface, zero config keys.

---

## 2. Ground truth (verified at 8f7139b — these anchors win over the plan's)

God functions in `src/commands/improve` over the 220 bar (AST-measured): `runImprovePreparationStage` preparation.ts:838-2390 (1553), `akmImprove` improve.ts:401-1309 (909), `akmReflect` reflect.ts:948-1623 (676), `runImproveLoopStage` loop-stages.ts:78-721 (644), `akmDistill` distill.ts:680-1312 (633), `akmExtract` extract.ts:905-1383 (479), `runImproveMaintenancePasses` loop-stages.ts:939-1408 (470, contains the 389-LOC anon at :1009), `planConsolidation` consolidate.ts:1387-1812 (426), `akmRecombine` (393, deleted), `processSession` extract.ts:552-901 (~350, **20** positional args — `standardsContext` was added post-plan), `handleMergeOp` consolidate.ts:2117-2413 (297), `runConsolidationPass` preparation.ts:229-527 (~299), anon recombine.ts:706 (287, deleted), `promoteMemoryToKnowledge` distill/promote-memory.ts:73-326 (254), `narrowConsolidationPool` consolidate.ts:1125-1376 (252), `handlePromoteOp` consolidate.ts:2477-2690 (214), improve-cli.ts `run` :165-373 (209), `runImprovePostLoopStage` loop-stages.ts:723-930 (208), `sanitizeReflectPayload` reflect.ts:686-892 (~207), `runSessionExtractPass` preparation.ts:529-734 (~206).

Already landed upstream — do NOT re-plan/re-churn:
- The reflect double disk-read collapse (single read at `reflect.ts:1020-1038`). (R3)
- `akmConsolidateInner` (:1938-2083) is already the narrow→plan→apply 3-pass shape with 4 exported op handlers dispatched via `ConsolidateOpContext` (:2092-2114, incl. the `generateMergedContentFn` seam :2113). (R5)
- The `EventsContext.db` fast path (`events.ts:254-266`) + `akmImprove`'s long-lived handle (`improve.ts:838-846`, commit 536d6f2): ~50 of 65 improve-area appendEvent sites are ctx-adopted. Remaining hot no-ctx sites: reflect.ts:953,:980,:1403,:1491,:1605; distill.ts:716,:1035,:1112,:1249,:1280; distill/promote-memory.ts:159,:266,:296; distill/quality-gate.ts:310; plus locks.ts:45 and extract.ts:765,:869,:1355 (dbPath-only ctx). (R28)
- The promotion-policy bench recompute (`tests/commands/distill/promotion-policy-corpus.ts` + `distill-promotion-policy.bench.test.ts:13-16`) — only the literal trim remains. (R21)
- Three prep pre-loop passes are already extracted (`runConsolidationPass` :229, `runSessionExtractPass` :529, `runValidationAndRepairPass` :736). (R1)

"LOOK/CHANGE separation" and "corrective-evidence" have NO code identifiers. Their code embodiments, for the preservation review: the signal-delta gate (`eligibility.ts:349-431` — `isSignalDeltaEligible` strict `fb > lastProposal`, feedback map counts only `metadata.signal/note`, proposal cursor moves on `reflect_invoked` always / `distill_invoked` only for queued|skipped|validation_failed) + the `preparation.ts:1030-1153` partition (the 2026-05-26 "54-ref simultaneous-reflect incident" fix), and within consolidate the read-only narrow/plan vs mutating apply pass boundary (dry-run returns before `applyConsolidationPlan`, :1968). (R31)

`superseded_by` (snake_case archive frontmatter, `consolidate.ts:863`) ≠ `supersededBy` (camelCase belief edge, `memory-belief.ts:135`). The preserve list means the former.

Salience keep-intact set (R36): `computeSalience` weight switch `salience.ts:353-367`, `upsertAssetSalience` #644 CASE non-lowering guard `:448-457`, `isContentEncodingRow` `:408`, `DEFAULT_TYPE_ENCODING_WEIGHTS` `:135`, `computeValenceScore` `feedback-valence.ts:75`, `evaluateCollapseAlerts`, deterministic ranking. The three `!== false` outcome-weight reads live at `distill.ts:751`, `salience.ts:356`, `preparation.ts:1747` — moved verbatim, never flipped (§9.3 parity flip is deferred). `updateAssetOutcome`'s only caller is `preparation.ts:1649`, reading only `result.outcomeScore` (:1659).

Chunk 0a oracles (all green at HEAD, all fixtures frozen): `tests/commands/consolidate/goldens-consolidate-ops.test.ts` (drives the four exported handlers directly; merge-1+1 primary and high-confidence contradict pinned byte-for-byte), `goldens-consolidate-journal.test.ts` (journal round-trip via real `akmConsolidate`, recovery matrix, all-hot early-exit with zero LLM calls, guard verdicts), `goldens-merge-plans.test.ts` (precedence byte-for-byte), `tests/commands/improve/goldens-signal-delta-gate.test.ts` (truth table + partition counts). The ops suite header names itself "the Chunk 7 DoD 5 oracle".

---

## 3. Work items (ordered; each ≈ one focused dev session)

### WI-7.1 — Delete the recombine/synthesis subsystem + the orphaned index-DB export
**testMode: deletion-gate** · dependsOn: — · est: ~−1,300 src / ~−2,750 test (reported under §12.1's residual row) · Reqs: R22, R23, R37 (rewiring constraints), R39/R41 (this sweep's pairing + inventory)

Tests first (enumerate before touching src):
- DELETE: `tests/commands/improve-recombine.test.ts`, `tests/commands/improve-recombine-promote.test.ts`, `tests/recombine-tuning.test.ts`, `tests/recombine-drain-accept.test.ts`, `tests/state-db/recombine-hypotheses.test.ts`.
- UPDATE: `tests/graph-db-rekey.test.ts:473-490` (drop the `getEntitiesByEntryIds` contract block), `tests/_helpers/graph-store.ts:68` (comment), `tests/standards-prompt-injection.test.ts` (drop the `buildClusterPrompt` import `:39` and its recombine case in the cases array `:120-123` + header comment `:22` — same commit as the recombine.ts deletion or the suite fails to resolve; the `buildProceduralPrompt` half `:38,:125-127` stays until WI-7.3), `tests/commands/default-improve-strategies.test.ts` + its snapshot, `tests/commands/improve-strategy-selection.test.ts`, `tests/improve-strategies.test.ts` (recombine strategy fields only — its hotProbation cases `:97-123` are WI-7.3's), `tests/improve-shared-helpers.test.ts:49` (processKey), `tests/config-process-roundtrip.test.ts` (recombine fields), `tests/commands/improve/collapse-detector.test.ts` (pass union), `tests/health-checks-characterization.test.ts:132` (advisory wording).
- KEEP GREEN (oracles): `tests/commands/consolidate/consolidate-incremental.test.ts` (pins the SURVIVING `getNeighborsByEntryId` consumer), all four 0a golden suites (zero fixture diffs), `tests/storage/sqlite-migrations.characterization.test.ts` (unchanged in this item — schema drop is WI-7.3).

Steps:
1. Delete `src/commands/improve/recombine.ts` (1,009), `src/storage/repositories/recombine-repository.ts` (290), `src/assets/improve-strategies/synthesize.json`, `src/assets/improve-strategies/recombine-only.json`, `src/assets/prompts/recombine-system.md`.
2. Unwire: `loop-stages.ts:69,:805-845` (pass invocation); `improve.ts:72,:146-150 (recombineFn seam),:319-320,:894,:1100,:1204`; `core/improve-types.ts:68 ('recombine' EligibilitySource),:254-279 (RecombineResult),:445-449`; `core/improve-result.ts:53,:169`; `core/events.ts:144-148 ('recombine_invoked')`; `shared.ts:47` (processKey union → `'procedural'` only until WI-7.3); `proposal/repository.ts:101,:121` + `drain-policies.ts:56-61` (per D11); `config-schema.ts:510-536,:605`; `config-types.ts:62`; `engine-semantics.ts:16,:19,:33`; `improve-strategies.ts:13,:15,:82,:85`; `default.json:17`; `src/assets/help/help-improve.md:46-47`.
3. Rewire the KEPT collapse detector (R37 — mint/score math and stores stay byte-identical): `loop-stages.ts:888-905` invocation becomes consolidate-only (pass always `'consolidate'`, acceptedActions no longer adds `lessonsPromoted`); narrow the WRITE-side pass unions at `collapse-detector.ts:270,:464` and `canaries-repository.ts:32` to `'consolidate'` while keeping READS tolerant of historical `'recombine'|'both'` row values; update advisory wording `health/advisories.ts:128,:172` and comment `health/types.ts:506`.
4. Delete `getEntitiesByEntryIds` (`src/indexer/db/db.ts:1109`). Do NOT touch `getNeighborsByEntryId` (`db.ts:896`) — record retained-with-reason (D1).
5. Migration 014 stays in place verbatim (append-only); the table drop lands in WI-7.3's migration 018.
6. Grep-gate (zero hits in src/, allowlist = migrations.ts 014 entry + WI-7.3's 018): `akmRecombine`, `RecombineResult`, `recombineFn`, `recombine_hypotheses`, `recombine_invoked`, `RecombineHypothesisRow`, `recombine-repository`, `deriveRecombineLessonRef`, `getEntitiesByEntryIds`, case-insensitive `recombin`. Do NOT gate on bare `synthesize` (legit uses: `reflect.ts:313`, #636 prompts, `consolidate.ts:2989`, health html, config-sources, local.ts, installed-stashes) — use `synthesize.json` + the `BUILTIN_IMPROVE_STRATEGY_NAMES` entries instead.
7. Start `docs/design/execution/chunk-7/ledger.md`: inventory of deleted files/symbols, the retained-with-reason row for `getNeighborsByEntryId`, ledger entry for D11 (orphaned legacy proposal sources), net-LOC.

Acceptance:
- Grep-gate tokens above = 0 hits in src (allowlist honored). (R22, R23, R41)
- `consolidate-incremental.test.ts` green; 0a golden fixtures byte-identical; `collapse-detector.test.ts` green with consolidate-only wiring. (R31, R37)
- `bun run check:fast` green; no edits to fixed points. (R40)
- Ledger started with recombine inventory + retained-with-reason. (R41)

### WI-7.2 — Delete the improve-loop lanes: self-consistency, P0-A, multi-cycle, exploration, ValenceScore.lane, review_pressure
**testMode: deletion-gate** · dependsOn: 7.1 · est: ~−800 src / ~−1,600 test · Reqs: R11, R12, R15, R16, R18 (code/type half — migration 018 is 7.3's), R19, R20 (exploration half), R35 (entries a+b+b2), R39, R42

Tests first:
- RETIRE with designations (same commit, per D15): `tests/commands/improve/goldens-self-consistency.test.ts` + `tests/fixtures/goldens/improve/self-consistency.json` + its DESIGNATIONS.json entry; `goldens-p0a-selection.test.ts` + `improve/p0a-selection.json` + entry. `tests/goldens-designations.test.ts` must stay green.
- DELETE: `tests/commands/improve/improve-multi-cycle.test.ts` (414 — drives the deleted cycle loop).
- UPDATE (anchor by symbol/describe NAME, never by line range — this file's ranges are traps):
  - `tests/commands/improve/ws4-gate-coherence.test.ts` — remove exactly the five EXPLORATION describes: `"exploration budget"` (:185), `"exploration budget exhaustion"` (:251), `"no-confidence candidates and exploration"` (:277), `"makeGateConfig exploration budget from config"` (:443), `"exploration budget restoration on failure"` (:597 — drives the `:325-327` slot-restore code 7.2 deletes). Do NOT touch the auto-tune/calibration describes — `"auto-tune ceiling"` (:298), `"per-phase auto-tune persistence"` (:364), `"per-phase calibration isolation"` (:515) — they are WI-7.3's; and the Chunk-6-owned `"per-phase threshold store (Migration 012)"` (:66) and `"makeGateConfig per-phase threshold resolution"` (:105) must stay green through the whole chunk.
  - `tests/commands/improve/outcome-loop.test.ts` — remove the `"updateAssetOutcome — review_pressure (#613)"` describe (:309-390), the `.reviewPressure` result reads (:333,:362,:388), the `reviewPressure` literal in the accumulator at `:583`, and the `review_pressure: 0` row-literal fields (:670,:689,:713,:731); drop `review_pressure` from all its INSERT column lists (+ header comment :9) in the same edit — safe before the 018 column drop (DEFAULT 0 tolerates omission).
  - `tests/commands/improve/monitor-liveness.test.ts` — drop the `review_pressure: 0` field from the typed `AssetOutcomeRow` row builders (:35; the type import at :19 keeps compiling after the field deletion). Surviving-machinery suite (R37 proxy-adequacy metrics): UPDATE, never delete.
  - `tests/commands/health-minting-rollup.test.ts` — shrink the ratified-lane-set pin at :121 to `["high-salience","proactive","signal-delta"]` and retire/rework the high-retrieval byLane test (:90-93). Decision: historical `lane='high-retrieval'` proposal rows no longer roll up as an enrichment lane (classify as non-enrichment) — a live reporting behavior change; one ledger sentence (step 8).
  - `tests/distill.test.ts` (:671-683) — switch the `eligibilitySource` attribution round-trip to a surviving lane (`'high-salience'`); the attribution contract itself is preserved. (The suite then rides WI-7.5's KEEP GREEN list as-updated-here.)
  - `minRetrievalCount` option census (the option dies with P0-A; every typed options literal passing it hits a tsc excess-property error — delete the option line at each site, mechanical): `tests/commands/improve-path-exists-guard.test.ts:137,:173,:216`; `tests/commands/improve-memory.test.ts:186,:904,:968,:1012`; `tests/commands/improve-memory-misc.test.ts:239,:621,:836,:916,:984,:1067`; `tests/commands/improve/proactive-maintenance-flow.test.ts:109,:136,:168` (also simplify the "ensure P0-A is NOT the path" comments); `tests/commands/improve/consolidate-min-pool-size.test.ts:63`; `tests/commands/improve/extract-min-new-sessions.test.ts:90,:192`; `tests/commands/improve/improve-planned-filepath.test.ts:133`; `tests/commands/improve/extract-strategy-gate.test.ts:97`; `tests/commands/improve/improve-eligibility.test.ts` (folded into its P0-A rework, incl. :754 cursor pattern); `tests/commands/improve/goldens-signal-delta-gate.test.ts:357` — EXPLICIT CARVE-OUT: test-SOURCE edits to this frozen-designation suite are permitted and required (drop the option line; `minRetrievalCount: 0` already neutralizes the lane, and the fixture's own note at `signal-delta-gate.json:36` anticipates surviving the P0-A deletion) while the FIXTURE bytes stay frozen byte-identical. (`goldens-self-consistency.test.ts:197,:241` and `goldens-p0a-selection.test.ts:153` die with their suites above.)
  - `tests/commands/improve/feedback-valence.test.ts` (:20 exact-shape toEqual loses `lane`); `tests/commands/improve/engine-plan-boundaries.test.ts:229` (`selfConsistencyThreshold`); `tests/config-process-roundtrip.test.ts` (exploration/maxCycles fields); `tests/commands/improve/improve-sync-resilience.test.ts` (maxCycles refs); `tests/proposal-gate-decision.test.ts` (exploration-budget reason strings only — gate mechanics are Chunk 6's).
- KEEP GREEN: `goldens-signal-delta-gate.test.ts` (fixture byte-identical; suite source edited only per the census carve-out above), `outcome-invariance.test.ts` (untouched here — #691 formula pins unchanged; it reads only `outcomeScore`, and its `:49` INSERT column list is updated in WI-7.3 with migration 018), `improve-no-hang`, `improve-budget-watchdog`, `improve-session-lifecycle`, `improve-sync`, `improve-dry-run-side-effects`, `improve-lock-invariants`, `improve-db-locking`, `proactive-maintenance-flow` (green after its census edit above).

Steps:
1. **Self-consistency (R11)** — real extents, wider than the plan's: `loop-stages.ts:116-173` (SC_THRESHOLD/SC_N/jaccardSimilarity/pickMajorityVote) AND `:308-365` including the winner-persist tail `:335-359` (createProposal with `reflect-sc-` sourceRun + cooldown diversion). Also sweep the SC-only surface: `improve.ts:203,:208` (`selfConsistencyThreshold`/`selfConsistencyN` options), `:270,:1492` (utilityScores plumbing in `ImproveRunContext`), and reflect's draftMode support existing solely for SC — option doc `reflect.ts:144`, no-op-vote guard `:1434`, synthetic-draft branch `:1535-1563`, the `draftMode` option itself. There is NO config-schema field (options-only — the plan's "AND config-schema" is inapplicable here; note in ledger).
2. **P0-A (R15)** — `preparation.ts:1270-1336` (threshold const :1270, candidate union :1276-1282, selection gate :1327-1330), noFeedbackPool deferral routing `:1094-1106,:1150-1152`, loop-set merge `:1521-1533`, `eligibilitySource='high-retrieval'` stamps `:1555,:2039`, summary log `:2335`; `improve.ts:186` `minRetrievalCount` option; `improve-cli.ts:201-202,:292` `--min-retrieval-count`; `'high-retrieval'` union member `core/improve-types.ts:61`, `health/types.ts:508` ENRICHMENT_LANES, `repository.ts:366` doc text. Grep `requireFeedbackSignal` (`improve.ts:181`) for non-P0-A consumers before touching it — remove only its P0-A-specific read at `:1533` if it has other duties.
3. **Multi-cycle (R16)** — per D12: delete the loop `improve.ts:914-1135` (single pass remains), `maxCycles` option+resolution+config knob (`config-schema.ts:653`), accumulators, fixed-point break, #662 inter-cycle git bank, re-collect guard, `cyclesRun` reporting; KEEP the four stage seams `:131-137`.
4. **Exploration (R12)** — SURGICAL, not the plan's stale :77-215 line-range (that range now interleaves Chunk-6-owned gate code). Delete only: `improve-auto-accept.ts:77-86` (explorationBudgetCount), `:178-185` (budget init), `:215-221` (isExploration + guard), `:233-235`, `:247-249` (eligibilitySource override), `:262`, `:275-278`, `:325-327` (slot restore), makeGateConfig's `:368-370,:383-387,:403-410,:421`; `loop-stages.ts:205-206` (candidateCount arg); `'exploration'` member `core/improve-types.ts:67`. LEAVE for Chunk 6: `runAutoAcceptGate` (:152), `recordGateDecision` (:193-203), suppression (:237-244), archive-on-validation-failure (:313-324), `getPhaseThreshold` (:31,:391-401). The KEPT drain engine (`proposal/drain.ts`) is untouched.
5. **review_pressure (R18)** — surgical inside `outcome-loop.ts` (the plan's :127-305 range interleaves KEPT outcome_score EMA/clip): constants `:131,:137`, row field `:148`, result field `:197`, warm-start `:229`, compute/decay `:266-275`, upsert column writes `:281,:290,:301`, return `:305`. The column itself survives until WI-7.3's migration 018 (DEFAULT 0 tolerates omission from INSERT/UPDATE).
6. **ValenceScore.lane (R19)** — `feedback-valence.ts:38 (STRONG_VALENCE_THRESHOLD),:41 (FeedbackLane),:62-67,:81,:87-92`. Consumers verified reading only `.valence`/`.attention` (`preparation.ts:1647-1655,:1894`).
7. **Config (R20, exploration half)** — `ImproveExplorationSchema` `config-schema.ts:839-853` + registration `:907`; verify passthrough leniency for orphaned user-config blocks (D16).
8. Ledger entries (R35): **(a)** SC default-ON deletion → 3×→1× reflect LLM calls on utility≥0.7 refs (intended); **(b)** P0-A with D13's corrected attribution wording; **(b2)** health minting-rollup: with `'high-retrieval'` removed from ENRICHMENT_LANES, historical rows with that lane no longer classify as enrichment in health reporting (intended, small). Grep-gate additions: `selfConsistencyThreshold`, `pickMajorityVote`, `jaccardSimilarity` (scope src/commands/improve), `explorationBudgetCount`, `high-retrieval`, `minRetrievalCount`, `maxCycles` (scope improve), `reviewPressure`, `FeedbackLane`, `STRONG_VALENCE_THRESHOLD`.

Acceptance:
- Grep-gate tokens = 0 in src (review_pressure column string permitted only in migrations until 7.3). (R11, R12, R15, R16, R18, R19, R41)
- `goldens-designations.test.ts` green with both entries removed; no other fixture touched. (R42)
- ws4 Chunk-6-owned describes (`"per-phase threshold store (Migration 012)"`, `"makeGateConfig per-phase threshold resolution"`) green; ws4 auto-tune describes untouched (7.3's); `proposal/drain.ts` untouched; `outcome-invariance` untouched and green. (R12, R18)
- tsc clean across the full `minRetrievalCount` census; `monitor-liveness`, `health-minting-rollup`, `distill.test.ts` updated per list; `signal-delta-gate.json` fixture byte-identical despite the suite-source edit. (R15, R18, R42)
- Ledger entries (a)+(b)+(b2) written with corrected P0-A wording. (R35)
- `bun run check:fast` green; safety suites green. (R40)

### WI-7.3 — Delete the opt-in/branch features: calibration+autotune, procedural, dedup #617, judged-cache #581, hotProbation, schemaSimilarity, proceduralAwareFloor; append migration 018
**testMode: deletion-gate** · dependsOn: 7.2 · est: ~−1,950 src / ~−2,000 test, +~60 (migration + content-hash module) · Reqs: R13, R14, R17, R18 (migration-018 half), R20 (calibration half), R35 (entries for default-ON deletions + extract capture), R39, R41

Tests first:
- DELETE: `tests/improve-calibration.test.ts`, `tests/commands/improve-procedural.test.ts`, `tests/commands/improve/dedup.test.ts`, `tests/commands/improve/dedup-cache-wiring.test.ts`, `tests/commands/consolidate/consolidate-judged-cache.test.ts`, `tests/commands/improve/hot-probation.test.ts`, `tests/commands/improve/schema-similarity-gate.test.ts`, `tests/issue-641.test.ts` (pins the deleted floor).
- UPDATE: `ws4-gate-coherence.test.ts` — remove exactly the three AUTO-TUNE/CALIBRATION describes by NAME: `"auto-tune ceiling"` (:298), `"per-phase auto-tune persistence"` (:364), `"per-phase calibration isolation"` (:515); the Chunk-6-owned describes (:66, :105) stay green (exploration describes already removed in 7.2). `tests/health-md-report.test.ts` (:8,:39 summarizeCalibration import; `:127` judgedCacheSkipped fixture field), `tests/health-ws5-observability.test.ts` (judgedCacheSkipped counter :103,:123,:173,:237,:259), `tests/commands/improve/improve-target-integration.test.ts` (remove the `perfTelemetry?.judgedCacheSkipped` assertion `:137` — or re-point it at a surviving perfTelemetry field — and the `hotProbation` config blocks `:82,:129`), `tests/improve-strategies.test.ts` (the hotProbation freeze/mutation cases `:97-123`; its recombine half was 7.1's), `tests/extract-command.test.ts` (incl. hotProbation plan cases `:876-942`), `tests/extract-watch.test.ts`, `tests/commands/improve/extract-triage-gate.test.ts` (keep default-path triage coverage), `tests/extract-prompt.test.ts` (capture fields, D4), `tests/standards-prompt-injection.test.ts` (`buildProceduralPrompt` import :38 + its case :125-127; the buildClusterPrompt half was 7.1's), `tests/config-process-roundtrip.test.ts`, `tests/coverage-hardening/core-config-walker.test.ts`, `tests/storage/sqlite-migrations.characterization.test.ts` (snapshot re-recorded once for 018), and the FULL migration-018 raw-SQL census per D5: `tests/integration/mv-durable-recovery.test.ts:116,:166,:286` + `tests/integration/goldens-mv-recovery.test.ts:134` + `tests/commands/mv.test.ts:1229,:1236,:1243` (INSERT column lists; fixture bytes unchanged) + `tests/commands/mv.test.ts:1278-1284` (semantic rework: keep the rename-carry contract on `retrieval_count`/`outcome_score`, drop review_pressure from SELECT/row type/expected objects) + `tests/commands/improve/outcome-invariance.test.ts:49` (INSERT column list only; #691 formula pins UNCHANGED).
- KEEP GREEN: all four 0a golden suites with ZERO fixture diffs (judged-cache/dedup removal must not perturb them — ops suite drives handlers directly; journal scenarios never populate `consolidation_judged`), `consolidate-op-handlers`, `consolidate-all-hot-skip`, `consolidate-incremental`.

Steps:
1. **Calibration + autoTune (R13, R17a, D3)** — delete `src/commands/improve/calibration.ts` (294); `maybeAutoTuneThreshold` `preparation.ts:121-204` (docblock from :99) + call sites `preparation.ts:505,:2355`, `loop-stages.ts:700-716`; re-export `improve.ts:80` + stale comment block `:494-510`; `calibration_autotune` event (`events.ts:122`); `persistPhaseThreshold` write-side wiring (`preparation.ts:22,:193`). Health cascade per D3 (types/metrics/improve-metrics/html-report/health/windows). Leave migration 012 + `makeGateConfig` phaseThreshold READS intact (Chunk 6).
2. **Procedural (R14, D4)** — delete `src/commands/improve/procedural.ts` (501) + `src/assets/prompts/procedural-system.md`; unwire `loop-stages.ts:68,:849-885`; `improve.ts:71,:152-156 (proceduralFn seam),:321-322,:895,:1101,:1205`; `improve-types.ts:69,:281-303,:451-455`; `improve-result.ts:54,:170`; `events.ts:150-155`; `config-schema.ts:537-546,:606`; `config-types.ts:63`; `engine-semantics.ts:34`; `shared.ts:47` (processKey union dissolves); `PROPOSAL_SOURCES 'procedural'` (D11); `default.json:19`. Delete the extract-side capture per D4 (`extract-prompt.ts:93-103,:223-229,:294-302`; `extract.ts:474-479,:842-845`) with ledger entry.
3. **dedup #617 (R17b)** — extract `cacheHash` + `stripFrontmatterBody` into new `src/commands/improve/content-hash.ts` (~40 LOC; kept consumers: `consolidate.ts:37,:494-495,:626-637,:912-930→dies,:2290,:2594-2601`, `distill.ts:82`); then delete `dedup.ts` (565) wholesale, the consolidate branch `consolidate.ts:1191-1233` + opts `:312-318`, wiring `preparation.ts:423-426`, config `config-schema.ts:323-342`. (This is the memory dedup pre-pass — Chunk 6's proposal dedup/cooldown in repository.ts is NOT touched.)
4. **judged-state cache #581 (R17c — DEFAULT ON, D2)** — delete `consolidate.ts:320-329` opts, `computeMemoryContentHash` `:908-935`, narrowing `:1273-1331`, recording `:1565,:1755,:1768-1803`, the `judgedCacheSkipped` perfTelemetry field (`consolidate.ts:240`) with its consumers (health surfaces + the three test files in the UPDATE list); `src/storage/repositories/consolidation-repository.ts` (77); config `config-schema.ts:343-351`. Ledger entry: unchanged memories are re-judged each consolidate run (more LLM calls) — live change, plan's default-off framing stale.
5. **hotProbation (R17d)** — delete `src/commands/improve/hot-probation.ts` (50); `extract.ts:75,:580,:851,:1152`; `consolidate.ts:38,:1153-1189` partition; config `:458-467`. SAFETY BOUNDARY: the `captureMode:'hot'` merge/delete guards (`consolidate.ts:2253,:2432-2443`) and indexer captureMode machinery (`ranking-contributors.ts:315-320,:475`, `passes/metadata.ts:145-147,:388`) protect user `akm remember` memories and MUST survive.
6. **schemaSimilarity (R17e — DEFAULT ON since R3, D2)** — delete `src/commands/improve/schema-similarity-gate.ts` (196); `extract.ts:277-287,:814-820,:1145-1167`; the `schemaSimilarityCtx` processSession arg; config `:440-457`. Ledger entry: live intake-gate change where derived embeddings exist.
7. **proceduralAwareFloor (R17f)** — `triage.ts:31-44,:98-99,:154-160,:184-191`; `extract.ts:567-568,:671`; config `:552-556`. The default-path triage sub-scores (toolDensity/editCommit) are KEPT.
8. **Config (R20)** — `ImproveCalibrationSchema` `config-schema.ts:817-836` + registration `:906`.
9. **Migration 018 (D5)** — append `018-drop-dead-lane-schema` exactly as specified in D5; re-record the characterization DDL snapshot once; update the FULL raw-SQL census from D5 (mv-durable-recovery, goldens-mv-recovery, mv.test.ts INSERTs + the :1278-1284 semantic rework, outcome-invariance :49) in the same commit.
10. Ledger + grep-gate additions: `maybeAutoTuneThreshold`, `computeThresholdAutoTune`, `summarizeCalibration`, `gateDecisionsToSamples`, `CalibrationSummary`, `calibration_autotune`, `akmProcedural`, `ProceduralCompilationResult`, `procedural_compiled`, `buildProceduralPrompt`, `orderedActions`, `runDeterministicDedup`, `consolidation_judged` (allowlist: migrations 007 + 018), `computeMemoryContentHash`, `hotProbation`, `isHotProbation`, `schemaSimilarity`, `proceduralAwareFloor`, `review_pressure` (allowlist: migrations 010 + 018). Do NOT gate on bare `calibration` (akm-eval judge-calibration harness) or bare `procedural` (historical issue refs).

Acceptance:
- Grep-gate tokens = 0 in src (allowlists honored); processSession arg count now 18 (20 at HEAD − `schemaSimilarityCtx` − `hotProbationEnabled`; `triage` stays as one arg with its `proceduralAwareFloor` field removed inside it). (R13, R14, R17, R41)
- 0a golden fixtures byte-identical; `consolidate-all-hot-skip`/`consolidate-incremental` green; captureMode:'hot' guards + indexer captureMode machinery untouched. (R31, R34, R17d)
- Migration 018 appended, no historical entry edited; migration-ID-pinning suites green; characterization snapshot re-recorded exactly once. (R18, R22, D5)
- Ledger entries for #581, schemaSimilarity, extract-capture written as live behavior changes. (R35)
- `bun run check` (incl. affected integration) green. (R40, R41)

### WI-7.4 — Mint the shared helpers: RunContext, proposal-envelope facade, serializeFrontmatterQuoted, single derived-ref impl, god-fn ratchet; DRY fixes + rename
**testMode: test-first** · dependsOn: 7.3 · est: +~550 new / −~120 dup; net ≈ +430 (the §5 ~+500 budget) · Reqs: R8 (mint), R24, R25, R26, R27 (D8 decision), R43, R9 (seam), R6 (gate minted), R7 (vocabulary home)

Tests first (WRITE these before implementation):
- `tests/core/asset-serialize.test.ts` — `serializeFrontmatterQuoted` contract: every scalar JSON.stringify-quoted; arrays serialize as `["a", "b"]` (distill's form — the canonical pick); key order preserved; round-trips through `parseFrontmatter`.
- NEW `tests/commands/improve/derived-ref.test.ts` — producer/consumer agreement: same parent resolved for (i) `source:` needing parseAssetRef normalization (whitespace/origin), (ii) `derivedFrom:`-keyed memories, (iii) `.derived` name suffix; PINS the intended widening: derivedFrom-keyed families now receive contradiction edges from `detectAndWriteContradictions` (plan §6's "producer/consumer cannot disagree").
- NEW `tests/commands/improve/proposal-envelope.test.ts` — facade emits through `createProposal` with arg-for-arg equivalence to a direct call (current shape: `payload.content` + optional frontmatter, status pending; NO beforeHash/FileChange fields exist).
- NEW `tests/commands/improve/run-context.test.ts` — the D6 memo contract, pinned: (i) scope — a memo created for one verb invocation/pass region never serves a later invocation (reflect/distill get a fresh memo per invoke; reads observably happen at invoke time, not prep time); (ii) write-through invalidation — `readAsset` after an in-run write of the same path (via `noteAssetWrite`/`writeAsset`) returns POST-write bytes; (iii) the prep-region memo is seeded only after the mutating pre-loop passes return.
- NEW `tests/architecture/improve-fn-size-ratchet.test.ts` — TS-AST scan (typescript devDep; precedent `tests/architecture/agent-runner-seam.test.ts`) of `src/commands/improve/**`: every function-like node (incl. arrows/anons) ≤220 lines unless in the exported shrink-only baseline (pattern: `tests/lint-isolation-ratchet.test.ts`); baseline = the current post-deletion offender list with recorded sizes; equality-asserted so it can only shrink.
- UPDATE `tests/commands/improve-result-to-file.test.ts` for the R43 renames.

Steps:
1. `src/commands/improve/run-context.ts` — `RunContext` carrying: `config`, `eventsCtx: EventsContext` (do NOT change EventsContext's shape — `loop-stages.ts:434,:440,:625,:627` dereference `eventsCtx.db` directly), `proposalsCtx`, `chat`/`getLlmConfig`, `sourceRun`, `dryRun`, `signal`, `now`, and the D6 read seam: `readAsset(pathOrRef)` with a memo that is pass-/invocation-scoped (never run-wide) plus the `noteAssetWrite(path)`/`writeAsset` write-through invalidation hook — see D6 for the binding scope rules (prep-region memo seeded post-pre-loop-passes; fresh memo per reflect/distill invocation; post-write reads return post-write bytes). No db handle into ProposalsContext (D14). Document the three-verb vocabulary here + in the facade (D17).
2. `src/commands/improve/proposal-envelope.ts` — `emitProposal(ctx, input)` facade per D10; wraps `createProposal` with the CURRENT shape. Adoption happens in WI-7.5/7.7/7.8 (five sites).
3. `serializeFrontmatterQuoted` in `src/core/asset/asset-serialize.ts` (module doc :66-87 already names this case); adopt at `distill.ts:492-497` and `content-repair.ts:94-96,:135-137,:158-160`. Note: content-repair array values (tags) change bytes from `["a","b"]` to `["a", "b"]` — verify no fingerprint golden embeds repaired-array frontmatter (grounding says none pins it). (R24; recombine's copy died in 7.1.)
4. Single keyed-on-ref `resolveParentRef`/`isDerivedMemory` (new `src/commands/improve/memory/derived-ref.ts`): semantics = `fm.source` via `parseMemoryRef` normalization → `fm.derivedFrom` via `makeAssetRef` → strip `.derived` suffix. Consumer (`memory-improve.ts:736,:739,:816-832`) adopts directly; producer (`memory-contradiction-detect.ts:127-154,:255-256`) converts filePath→ref via its existing `toMemoryRef` (:120-125) then calls the shared impl. Producer-side widening is intended and pinned by the new test. (R25)
5. Single `MAX_REJECTED_PROPOSALS = 3` (export from an improve shared module); adopt at `reflect.ts:231/:285` and `distill.ts:908/:911`. (R26)
6. R27 per D8: no code change; ledger records the row as mis-grounded (block :728-735 read-only; block :313-333 body-mutating, outside mutateFrontmatter's contract; `resolve-relative-dates.json` frozen golden pins its bytes).
7. R43 renames: `relativeImproveResultPath` → `improveRunLocator`, `writeImproveResultFile` → `recordImproveRunResult` (matching sibling `recordTerminatedImproveRun`, `improve-result-file.ts:146`); update `improve-cli.ts:25-27,:351-361` (stderr warning keeps its semantics) and the test file. File rename of `improve-result-file.ts` itself is optional; if renamed, keep it mechanical.

Acceptance:
- All six tests-first suites green; helpers exist with exactly one implementation each (`grep -c serializeFrontmatterQuoted src` > 0, duplicate serializer bodies = 0; `resolveParentRef` defined once). (R8, R24, R25, R26)
- `run-context.test.ts` pins the D6 memo contract: invoke-time reads (fresh memo per verb invocation) AND post-write bytes after in-run writes of the same path. (R9, D6)
- Ratchet meta-test green with the recorded baseline; baseline file reviewed as the WI-7.5–7.8 worklist. (R6)
- Net add within the ~+500 §5 budget (reported). (R8, R41)
- No change to `EventsContext` shape, `core/asset/frontmatter.ts`, or ProposalsContext. (D8, D14, R28)

### WI-7.5 — Decompose the revise (reflect) + learn (distill) verbs; trim the promotion-policy literal; adopt structured-call + events ctx in the reflect/distill subtree
**testMode: characterization-preserve** · dependsOn: 7.4 · est: ~1,300 LOC reorganized; net ≈ −900 (incl. −820 literal) · Reqs: R3, R4, R21, R28 (14 subtree sites), R30 (reflect/distill files), R36 (distill read), R9 (adoption), R6 (progress), R7 (naming)

Tests first:
- RESHAPE BEFORE TRIMMING (R21): `tests/commands/distill/distill-promotion-policy.bench.test.ts:13-16` — replace the full deep-equal against the frozen constant with (a) recompute via `selectPromotionPolicy(DEFAULT_PROMOTION_POLICY_CORPUS)` and (b) equality on `selectedModel.{name,threshold}` against the trimmed constant; `tests/commands/distill/distill-promotion-policy.test.ts:23-44` — trainingSize/heldOutSize/heldOut-metrics/baselines assertions switch from `getDefaultPromotionPolicySelection()` to live recompute; `:64` (.selectedModel.name) survives.
- KEEP GREEN (the characterization oracle): `tests/commands/reflect/reflect-size-gate.test.ts`, `reflect-noise-gate`, `reflect-propose`, `reflect-unattended-pin`, `reflect-completed-on-failure`, `reflect-pipeline-fixes`, `tests/commands/reflect-response-schema.test.ts`, `tests/commands/improve-reflect-unsupported-type-skip.test.ts`, `tests/issue-636-reflect-synthesize-description.test.ts`; `tests/distill.test.ts` (as updated in 7.2 for lane attribution; incl. :311-333 Reflexion injection, :1016 assessment), `tests/commands/distill/promote-memory.test.ts`, `distill-content-repair`, `distill-response-schema`, `distill-cli-flag`, `tests/commands/improve/quality-gate-fail-closed.test.ts`, `distill-guards`, `tests/commands/improve-distill-planner-skip-lessons.test.ts`; `tests/llm/structured-call.test.ts` + adopter suites.

Steps:
1. **akmReflect (R3)** → thin orchestrator over named passes: `source-snapshot-read` (the existing single read `:1020-1038`, routed through `RunContext.readAsset`; index-lookup fallback + `options.assetContent` seam preserved; do NOT re-plan the landed double-read collapse), `revise-propose` (LLM call → sanitize → `emitProposal` facade at the `:1583` site), `size-gate` (the `checkReflectSize` call-site inside a slimmed `sanitizeReflectPayload` — the pure check stays in `proposal/validators/proposal-quality-validators.ts:283`; splitting the size/noise gates out takes `sanitizeReflectPayload` under the bar). draftMode surface is already gone (7.2).
2. **akmDistill (R4)** → orchestrator over: `extract-candidates` (the plan's name has no literal counterpart — DEFINED HERE as the input-resolution + refusal/type-guard region `:710-:764` incl. the single input read at `:764` via `readAsset`; the in-loop salience frontmatter stamp at `:816` writes that same file and MUST route through `writeAsset`/`noteAssetWrite` so the invocation-scoped memo serves post-write bytes — D6), `promote-policy-apply` (the `:869-900` memory→knowledge branch delegating to `promoteMemoryToKnowledge`, reading ONLY the frozen `{selectedModel}`), `distill-propose` (rejected-context `:908-915` + LLM + `emitProposal` at `:1231`). Decompose `promoteMemoryToKnowledge` (254) under the bar. The `outcomeWeightEnabled` read at `distill.ts:751` moves verbatim and remains the single read-site feeding quality-gate/promote-memory threading (R36; no §9.3 flip).
3. **Promotion-policy trim (R21)** — `DEFAULT_PROMOTION_POLICY_SELECTION` (`distill-promotion-policy.ts:650-1491`, 842 lines) shrinks to a frozen `{selectedModel}`-only constant (~8 lines) with a new narrow type; `SELECTED_MODEL`/`DEFAULT_PROMOTION_POLICY` derivations (:1493-1502) keep working; `getDefaultPromotionPolicySelection` (:1504-1506, test-only callers) is removed. The bench recompute infra already exists — do NOT re-extract the corpus.
4. **Events ctx (R28)** — thread `RunContext`/`EventsContext` into the 14 no-ctx appendEvent sites: `reflect.ts:953,:980,:1403,:1491,:1605`; `distill.ts:716,:1035,:1112,:1249,:1280`; `distill/promote-memory.ts:159,:266,:296`; `distill/quality-gate.ts:310`.
5. **Structured-call (R30, D7)** — migrate `reflect.ts:904,:1483` and `distill.ts:740,:992` onto `callStructured`; existing `chat` option seams keep working (their shape matches `CallStructuredRequest.chat`); `_setChatCompletionForTests` behavior verified.
6. Naming: reflect passes live under the revise vocabulary, distill under learn (module docs/pass names only — D17). Ratchet baseline shrinks: `akmReflect`, `akmDistill`, `sanitizeReflectPayload`, `promoteMemoryToKnowledge` removed.

Acceptance:
- Every keep-green suite passes unchanged (fixtures untouched); reshaped policy tests green BEFORE the trim commit. (R3, R4, R21)
- `DEFAULT_PROMOTION_POLICY_SELECTION` literal ≤ ~10 lines; production reads only `.selectedModel.{name,threshold}` (grep). (R21)
- Zero raw `chatCompletion` calls remain in reflect.ts/distill.ts (grep); zero no-ctx `appendEvent(` in the reflect/distill subtree (grep for `appendEvent({` without ctx arg). (R28, R30)
- Ratchet baseline shrunk by the four functions; no new fn >220. (R6)
- Outcome-weight reads semantically unchanged (`!== false` at the moved sites). (R36)

### WI-7.6 — Decompose runImprovePreparationStage into the six named passes on RunContext
**testMode: characterization-preserve** · dependsOn: 7.4 (order after 7.5) · est: ~1,400 LOC reorganized; net ≈ −600 · Reqs: R1, R36, R9 (adoption), R6 (progress)

Tests first (all KEEP GREEN — this item adds no behavior):
- `tests/commands/improve/goldens-signal-delta-gate.test.ts` (FROZEN — partition counts + truth table byte-identical), `improve-eligibility.test.ts`, `salience-wiring.test.ts`, `outcome-invariance.test.ts` (#691 formula pins), `outcome-loop.test.ts` (post-7.2 shape), `improve-triage-prepass.test.ts`, `proactive-maintenance-flow.test.ts`, `consolidate-min-pool-size.test.ts`, `engine-plan-boundaries.test.ts`, `encoding-salience.test.ts`, `collapse-detector.test.ts`, `anti-collapse.test.ts`, `tests/ranking-salience-boost.test.ts`, `feedback-valence.test.ts`.

Steps (carve the 1,553-line stage body at `preparation.ts:838-2390` into six named pass functions on `RunContext`; the three already-extracted pre-loop passes stay):
1. `snapshot-manifest` — timestamp-map build (:1055-1079) + creation of the PREP-REGION `readAsset` memo, seeded only AFTER the three mutating pre-loop passes have returned (`runConsolidationPass`'s op writes are already on disk); frontmatter re-reads during validation/ranking (:769,:886) route through it. The memo is discarded at stage end and is NEVER handed to reflect/distill — they get fresh per-invocation memos (D6: read-once within a scope, no hashing, write-through invalidation if any in-region write occurs).
2. `candidate-gather` — the signal-delta partition into eligibleRefs/distillOnlyRefs/noFeedbackPool/fullySkipped (:1087-1265; the 2026-05-26 fix comment at :1032-1033 and partition semantics move VERBATIM), proactive-maintenance selector (:1338-1386), high-salience/#608 + forgetting-safety rescue (:1495-1533,:2030-2045). P0-A is already gone (7.2).
3. `salience-score` — outcome scoring loop (:1622-1663, `updateAssetOutcome` caller reading only `.outcomeScore`) + `computeSalience`/`upsertAssetSalience` wiring (:1745-1810). R36: machinery moved verbatim — the `:1747` `!== false` read stays the single read-site; #644 guard, `isContentEncodingRow`, weights, tables untouched.
4. `valence-score` — the `computeValenceScore` call sites (:1647-1655 `.valence`, :1894 `.attention`); `feedback-valence.ts` machinery untouched (minus `.lane`, gone in 7.2).
5. `standards-context` — locate and extract the standards-context assembly currently inline in the stage (threaded onward to processSession/reflect).
6. `eligibility-filter` — replay budget (:2074) + deterministic ranking + summary emit (:2212-2390).
7. `runImprovePreparationStage` becomes a thin orchestrator (<200) sequencing the six passes; trim `runConsolidationPass` (~299) and `runSessionExtractPass` (~206) under the bar. Ratchet baseline shrinks accordingly.

Acceptance:
- All keep-green suites pass; `signal-delta-gate.json` fixture byte-identical. (R1, R31, R38)
- Six pass functions exist with the plan's names; orchestrator <200; ratchet baseline shrunk by `runImprovePreparationStage`, `runConsolidationPass`, `runSessionExtractPass`. (R1, R6)
- Salience/outcome/valence machinery diff is move-only (reviewer checks the three `!== false` reads and the #644 guard verbatim). (R36)
- No new file reads outside `readAsset` in the six passes (grep `readFileSync` in the pass modules); the prep-region memo is created post-pre-loop-passes, discarded at stage end, and never escapes into the verb invocations (D6 scope rules — `run-context.test.ts` pins the contract). (R9)

### WI-7.7 — Decompose akmImprove + loop-stages + akmExtract; RunContext into processSession; land the appendEvent hot-path test
**testMode: characterization-preserve** (R29 test is test-first) · dependsOn: 7.5, 7.6 · est: ~1,600 LOC reorganized; net ≈ −450 · Reqs: R2, R10, R28 (remainder), R29, R30 (extract), R6 (progress)

Tests first:
- NEW (WRITE FIRST, R29): `tests/commands/improve/appendevent-hot-path.test.ts` — asserts improve hot paths never hit appendEvent's slow (double-open) path. Sanctioned techniques (mock.module is lint-banned; sandbox/_preload frozen): (a) handle-identity DI per `improve-db-locking.test.ts:158,:220` — inject `eventsCtx: {db}` and assert every stage/verb callee received the identical handle; (b) counting-proxy `Database` as `EventsContext.db` — drive `akmReflect`/`akmDistill`/stage fns with a RunContext carrying the proxy and assert all event rows land in the injected handle (slow path would write elsewhere/open new connections). Prefer DI over adding a production counter seam; if a seam proves unavoidable, flag it to reviewers against §1.3.
- KEEP GREEN: `improve-no-hang`, `improve-budget-watchdog`, `improve-session-lifecycle`, `improve-sync`, `improve-sync-resilience`, `improve-dry-run-side-effects`, `improve-lock-invariants`, `improve-db-locking`, `tests/extract-command.test.ts`, `tests/extract-session-tracking.test.ts` (#602 skip lives INSIDE processSession), `extract-min-new-sessions`, `extract-strategy-gate`, `extract-triage-gate`, `tests/session-indexing.test.ts`, `tests/extract-prompt.test.ts`, `tests/extract-watch.test.ts`.

Steps:
1. **akmImprove (R2)** — cycle loop already deleted (7.2); extract the remaining god-fn mass into named units: run-setup (lock acquire `locks.ts` path, index+collect closure `:536`, long-lived `eventsDb` open `:838-846`), stage sequencing via the KEPT #616 seams (D12), run-teardown (sync/error/catch-path plumbing, result recording via the renamed `recordImproveRunResult`). Orchestrator <200. Build the internal args on `RunContext`; `ImproveRunContext` (:1476) either narrows onto it or is absorbed — without changing `EventsContext` shape.
2. **processSession (R10)** — signature `extract.ts:552-593` collapses from 18 positional args (20 at HEAD − schemaSimilarityCtx − hotProbationEnabled, both removed in 7.3) to `(runCtx: RunContext, session: SessionInput)`; SessionInput = per-session values `{sessionRef, prior, force}`; run-scoped values (harness, stashDir, config, getLlmConfig, chat, proposalsCtx, sourceRun, dryRun, timeoutMs, maxTotalChars, minContentChars, triage, sessionIndexing, signal, standardsContext) ride RunContext. Sole caller `extract.ts:1225` updated. Decompose `akmExtract` (905-1383): candidate discovery, per-session loop, summary — each <220.
3. **loop-stages** — `runImproveLoopStage` (post-deletion ~490) → per-ref pass sequence (reflect→distill invocation via existing `reflectFn`/`distillFn` seams, budget/watchdog, outcome events); `runImprovePostLoopStage` (208) trimmed; `runImproveMaintenancePasses` (470, incl. the 389-LOC anon at :1009) → named maintenance passes (memory cleanup, contradiction detect, collapse detector, retention purges — the pre-existing `purgeOld*` retention calls are NOT lifecycle machinery; keep verbatim). `improve-cli.ts` `run` (209) trimmed.
4. **Events ctx remainder (R28)** — `locks.ts:45` and extract's 3 sites (`:765,:869,:1355`) take the run EventsContext (extract's dbPath-only ProposalsContext-as-ctx replaced for event emission; per D14 proposal WRITES keep their current path).
5. **Structured-call (R30)** — migrate `extract.ts:1010,:1118` onto `callStructured`.
6. Ratchet baseline shrinks: `akmImprove`, `akmExtract`, `processSession`, `runImproveLoopStage`, `runImprovePostLoopStage`, `runImproveMaintenancePasses`, the loop-stages anon, improve-cli `run`.

Acceptance:
- R29 test green and meaningful (fails if a subtree site reverts to no-ctx). (R29)
- processSession = 2 params; all keep-green suites pass. (R10, R2)
- Zero no-ctx `appendEvent(` sites under src/commands/improve (grep); zero raw `chatCompletion` in extract.ts. (R28, R30)
- Ratchet baseline shrunk; only consolidate-family functions remain. (R6)
- `eventsCtx.db` deref sites (`loop-stages.ts:434,:440,:625,:627` equivalents) still compile against unchanged EventsContext. (R28)

### WI-7.8 — Decompose the consolidate ops into named passes; finish structured-call; final gates, ledger, grep-gate, net-LOC report
**testMode: characterization-preserve** · dependsOn: 7.7 · est: ~950 LOC reorganized; net ≈ −300 · Reqs: R5, R31, R34, R38, R30 (consolidate/shared/memory files), R32, R33, R35 (finalize), R40, R41 (final), R42 (frozen check), R6 (baseline emptied), R7 (audit), R37 (audit)

Tests first (the byte-for-byte oracle — ALL KEEP GREEN with ZERO fixture diffs):
- `tests/commands/consolidate/goldens-consolidate-ops.test.ts` (imports `handleMergeOp`/`handleDeleteOp`/`handlePromoteOp`/`handleContradictOp`, `ConsolidateOpContext` incl. `generateMergedContentFn` seam, op types from `src/commands/improve/consolidate`, `MemoryEntry` from `consolidate/types` — this export surface MUST stay resolvable; re-exports fine, import-path updates in the suite permitted, fixture bytes frozen), `goldens-consolidate-journal.test.ts` (journal helpers are module-private, observed via `spyOn(fs.writeFileSync/copyFileSync)` — if decomposition moves them, interception points may need mechanical updates; fixtures are the oracle), `goldens-merge-plans.test.ts`, `goldens-signal-delta-gate.test.ts`; plus `consolidate-op-handlers`, `consolidate-chunks`, `consolidate-pipeline-fixes`, `consolidate-writer-fixes`, `consolidate-all-hot-skip`, `consolidate-incremental`, `consolidate-eligibility`, `consolidate-promote-dedup`, `tests/commands/goldens-duration-flags.test.ts` (parseSinceToIso at `consolidate.ts:2824` is Chunk 9 scope — UNTOUCHED).

Steps:
1. **planConsolidation (:1387-1812, 426) (R5)** → named passes mirroring the existing narrow/plan/apply shape: llm-config-resolve (:1412-1415), chunk-sizing (:1427-1429), similarity-clustering (:1443-1447), anti-collapse-injection (:1454-1496), chunking (:1498-1501), pending-hash-preload (:1509), cold-start-budget (:1524-1545), per-chunk-judge loop (:1581-1766 — the abort-rate policy, all-hot early-exit `:1627-1634`, and the 2026-05-26 accounting invariant `processed == actioned + judgedNoAction + Σ skipReasons + failedChunkMemories` incl. per-secondary skip emission and failed/aborted-chunk bucketing move VERBATIM; counter increment points must not move), merge-plans reconcile (:1807-1809). Judged-cache recording is already gone (7.3).
2. **handleMergeOp (:2117-2413, 297)** → passes: skip-accounting closure, pre-flight existence/hot guard (:2132-2194), content-generate (:2205-2213), validate (:2227-2251), post-generation hot re-check (:2259-2274 — KEEP BOTH hot checks; they differ behaviorally (pre-flight avoids the LLM call and changes the skip reason; post-gen catches participants turning hot mid-run) and the golden refusal matrix pins the pre-flight path — do NOT dedupe), provenance/anti-collapse (:2276-2358), write/archive/journal/accounting (:2360-2412 — `archiveMemory` :838 semantics incl. `superseded_by` byte-identical).
3. **handlePromoteOp (:2477-2690, 214)** → the six-gate chain as named gate helpers in EXACT current order (within-run dedup :2492, pending-by-target :2518, destination-exists :2526, sanitization :2546, superseded refusal :2557, <100-char floor :2575, body-hash dedup :2596, frontmatter validation :2624, slug-variant pre-emit :2656) ending in the `emitProposal` facade at the `:2668` site. `handleDeleteOp`/`handleContradictOp` are already small — untouched beyond the facade/ctx.
4. Trim `narrowConsolidationPool` (:1125-1376, 252) under the bar; `getNeighborsByEntryId` consumption at :2859 unchanged (D1).
5. **Structured-call (R30)** — migrate `consolidate.ts:1660,:3013`, `shared.ts:71`, `memory/memory-contradiction-detect.ts:222`.
6. Facade adoption completes (consolidate promote site); vocabulary audit (D17): consolidate passes named under the consolidate verb; revise/learn naming from 7.5 checked; no machinery added.
7. **Final chunk gates**:
   - Empty the ratchet baseline; flip the meta-test to the absolute assertion (no fn >220 in `src/commands/improve/**`, no allowlist). (R6)
   - Run the full grep-gate (7.1/7.2/7.3 token lists) and record zero-counts in the ledger. (R41)
   - R32 audit: scoped grep for new states/water-marks/pressure computation/intake blocking/CAS archive/sandbox gate/purge-quarantine COMMANDS — pre-existing retention `purgeOld*` calls, `memory-belief.ts:14` beliefState convention, and `schema-similarity-gate` history are allowlisted vocabulary; `review_pressure` count must be ZERO (it was deleted), not merely flat. R33 audit: no new labeling/clamps/prompts/digests/trust records (the standalone consolidate HTTP confirm at :1998-2024 is pre-existing, preserved, not new).
   - Finalize `docs/design/execution/chunk-7/ledger.md`: deletion inventory per item; behavior-change ledger — (a) SC 3×→1× (intended), (b) P0-A with D13 corrected attribution, (c) dedup/cooldown→fingerprints noted as Chunk 6's entry NOT this chunk's, (d) recombine deletion per §13.1, (e) judged-cache #581 default-ON, (f) schemaSimilarity default-ON, (g) procedural extract-capture, (h) PROPOSAL_SOURCES orphaning, (i) R27 row mis-grounded, (j) `getNeighborsByEntryId` retained-with-reason; net-LOC REPORTED (~−3,900 chunk area + ~−1,300 residual) — never a pass/fail threshold.
   - Full `bun run check` green (typecheck + unit + affected integration, serial invocations, `--timeout=30000` on any direct `bun test`); safety suites green; `git diff` empty on the four fixed-point paths. (R40, R41)
   - Confirm no frozen fixture changed anywhere in the chunk (`git diff --stat tests/fixtures/goldens` shows only the two retired fixtures + DESIGNATIONS.json from 7.2). (R42)

Acceptance:
- All 0a golden suites green, fixtures byte-identical (merge/delete/promote/contradict outcomes, journal round-trip, hot-capture guard, contradiction preserve-and-qualify — byte-for-byte where deterministic). (R31, R34, R38)
- No fn >220 in improve; baseline empty. (R6, DoD 5)
- R32/R33 audits recorded clean; ledger finalized and committed; grep-gate zero-counts recorded. (R32, R33, R35, R41)
- `parseSinceToIso` and its frozen golden untouched; export surface for the ops suite intact. (R42, R34)
- Canary probe/canary_queries/encoding-salience/scoped-utility/outcome-loop(minus review_pressure) untouched beyond 7.1's sanctioned rewiring. (R37)

---

## 4. Gate checklist (manifest gates → items)

| Gate | Satisfied by |
|---|---|
| §5 behavior-change ledger attached to the chunk | Entries written in 7.2 (SC, P0-A-corrected) and 7.3 (#581, schemaSimilarity, extract-capture); D11/D8 notes in 7.1/7.4; finalized + committed in 7.8 (R35, R41) |
| Consolidate behavior preservation vs Chunk 0a goldens (§12.3, DoD 5) | Frozen fixtures byte-identical through 7.1 (collapse rewire), 7.3 (judged-cache/dedup removal), 7.6 (signal-delta), 7.8 (op decomposition + replay) (R31, R34, R38) |
| No lifecycle state model (§6, §12.4) | Standing prohibition on every item; scoped audit in 7.8; review_pressure grep → 0 (R32; plus R33 no-trust audit) |
| No god fn >~200 LOC remains in improve (DoD 5) | Ratchet minted 7.4 (220-line bar, whole directory incl. anons); shrunk 7.5/7.6/7.7; emptied + flipped absolute in 7.8 (R6) |
| §15.3 safety suites green + fixed points untouched (chunk-process) | Verified at every item boundary; final run + fixed-point diff check in 7.8 (R40, R41) |

## 5. Requirements coverage

R1→7.6 · R2→7.7 · R3→7.5 · R4→7.5 · R5→7.8 · R6→7.4 (gate) + 7.5/7.6/7.7/7.8 (satisfy) · R7→7.4 + 7.5 + 7.8 · R8→7.4 (mint) + 7.5/7.7/7.8 (adopt) · R9→7.4 (seam, D6) + 7.5/7.6 (adopt) · R10→7.7 · R11/R12/R15/R16/R19→7.2 · R18→7.2 (code/type) + 7.3 (migration 018 + raw-SQL census) · R13/R14/R17→7.3 · R20→7.2 + 7.3 · R21→7.5 · R22/R23→7.1 · R24/R25/R26/R27/R43→7.4 · R28→7.5 + 7.7 · R29→7.7 · R30→7.5 + 7.7 + 7.8 · R31/R34/R38→7.8 (kept green throughout) · R32/R33→all items, audited 7.8 · R35→7.2 + 7.3 + 7.8 · R36→7.5 + 7.6 · R37→7.1 + 7.8 · R39→7.1 + 7.2 + 7.3 · R40/R41→every item, final in 7.8 · R42→7.2 + 7.8

## 6. Top risks

1. **`getNeighborsByEntryId` deletion would break preserved behavior** — D1 resolves it (keep; ledger retained-with-reason). Any reviewer applying the manifest's §14.3 text literally must be pointed at `consolidate.ts:2859`.
2. **Two "default-off" deletions are live changes** (#581 judged-cache, schemaSimilarity — both `!== false` at HEAD). D2: proceed with ledger entries; reviewers sign off via DoD 6, not silently.
3. **Line-range deletions are traps**: exploration (:77-215 interleaves Chunk-6 gate code), review_pressure (:127-305 interleaves kept EMA), SC (plan ranges miss the winner-persist tail + reflect draftMode), ws4-gate-coherence (three owners share one file — anchor by describe NAME: exploration=7.2, auto-tune/calibration=7.3, migration-012/threshold=Chunk 6 keep-green). Every deletion in 7.2/7.3 is symbol- or describe-name-anchored, not range-anchored.
4. **Frozen-fixture discipline**: only two fixtures may leave the tree (retired with lanes, DESIGNATIONS.json same commit); every other golden byte-identical; `AKM_UPDATE_GOLDENS` never set except the single characterization-snapshot re-record in 7.3. Frozen designation binds the FIXTURE bytes, not the suite source: mechanical test-source edits to `goldens-signal-delta-gate.test.ts` (minRetrievalCount census) and `goldens-consolidate-journal.test.ts` (spy interception points) are permitted and sometimes required.
4b. **Option/type deletions have a wide tsc blast radius**: deleting `minRetrievalCount` alone touches 12 test files (census in 7.2); `AssetOutcomeRow.review_pressure`, `judgedCacheSkipped`, and `'high-retrieval'`/ENRICHMENT_LANES each break typed literals in suites the plan never names. The census lists in 7.1/7.2/7.3 are the inventory — run `bunx tsc --noEmit` immediately after each symbol deletion and reconcile any NEW breakage against the census before proceeding (a hit outside the census means the inventory was incomplete: stop and extend it, don't ad-hoc fix).
4c. **D6 memo staleness is the silent failure mode**: a run-wide readAsset memo would serve prep-time bytes after mid-run writes (`distill.ts:816`, accepted-proposal applies, consolidate op writes) and no golden pins write-then-reread. The scope rules + write-through invalidation in D6 are binding; `run-context.test.ts` is the pin.
5. **Append-only migrations**: 007/010/014 never edited; one appended 018; characterization snapshot re-recorded exactly once.
6. **Test invocation**: `--timeout=30000` mandatory; serial runs only.
7. **Stale plan baselines** must not be re-claimed: appendEvent fast path is ~50/65 adopted already (R28 scope = reflect/distill subtree + locks + extract); structured-call has 3 adopters (finish, don't deliberate); processSession has 20 args.
8. **Grep false positives**: never gate on bare `synthesize`/`calibration`/`procedural`/`pressure`; use the precise token lists in 7.1/7.2/7.3.
9. **Scope discipline**: no lifecycle/trust machinery, no §9.3 parity flip, no parseSinceToIso/Chunk-9 touches, no _set*ForTests retirements beyond what rewrites naturally absorb (18-seam census is Chunk 9's number), no ProposalsContext db handle (D14).
