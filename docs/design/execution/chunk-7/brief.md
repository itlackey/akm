# Chunk 7 Implementation Brief — improve decomposition + dead-lane deletions

Branch: `akm-090/chunk-7` (cut from `akm-090/wave-1`) · Wave 1, order 2 · Net-LOC target (REPORTED, never gated): ~−3,900 improve/memory/salience + ~−1,300 recombine/orphan-export fold-in (ledgered under §12.1's residual row, outside the ~−3,900).

*Revision 3 — requirement traceability re-based onto the R1–R37 execution inventory (this task's inventory), preserving Revision 2's adversarial-verification grounding (D6 memo scope/invalidation test-pinned; migration-018 raw-SQL census; processSession arg count; ws4-gate-coherence anchored by describe name; consumer censuses for recombine, high-retrieval, minRetrievalCount, review_pressure, judged-cache/hotProbation). R13 (self-confidence authorization) scope split with Chunk 6 is stated as a binding decision (D3) with the plan-internal §5-vs-§4.5 overlap flagged as a top risk. Plan-§5 deletions with no distinct inventory id (default-off branches, MAX_REJECTED_PROPOSALS dedup, the improve-result-file rename, the mutateFrontmatter row) are folded into work items and attributed to plan §5, not given phantom Rxx ids.*

**Authority.** `docs/design/akm-0.9.0-bundle-adapter-architecture-plan.md` is THE plan. Nothing in current code supersedes it. Where the plan says *preserve behavior*, current behavior is the oracle; everywhere else the plan is. All line anchors below were re-measured on the wave-1 worktree and win over the plan's stale anchors.

**Hard rules (plan §1.3 — violations are review BLOCKERS).**
- NO new trust/approval/security machinery: no labeling, no action clamps, no confirm prompts, no digests, no trust records. (R11, R33)
- Memory lifecycle is DEFERRED entirely (plan §6): no operational states, no water-marks, no pressure computation, no intake blocking, no CAS archive, no sandbox gate, no purge/quarantine commands. Anything lifecycle-shaped is out of scope BY DESIGN — do not "helpfully" add it. (R11, R33)
- Deletion is gated by inventory + zero-count greps, never by a LOC number; net-LOC is reported, not gated. (R30, R35)
- §15.3 safety suites stay green at every item boundary. Fixed points — `tests/_helpers/sandbox.ts`, `tests/_preload.ts`, the mock.module-ban lint (`scripts/lint-tests-isolation.ts` Rule 6 + its ratchet baseline), the hand-rolled sharding (`scripts/run-test-shard.sh`, `scripts/test-unit.sh`) — are UNTOUCHED. (R34)

**Operational traps (read before running anything).**
- `bun test <paths>` MUST carry `--timeout=30000` (bun 1.3.x ignores bunfig `[test]` timeout; the 0a golden suites run 12–18s and fail spuriously at the 5s default, then trip the `[sandbox tripwire] leaked env vars` error). Or go through `scripts/test-unit.sh` / `scripts/run-test-shard.sh`.
- Never run two `bun test` invocations concurrently in the same workspace (produces hundreds of false failures).
- Golden fixtures regenerate only under `AKM_UPDATE_GOLDENS=1`. Every fixture is registered in `tests/fixtures/goldens/DESIGNATIONS.json`, policed by `tests/goldens-designations.test.ts` — registry entries must be edited in the SAME commit as any fixture/consumer deletion, in both directions.
- Code comments in this area lie: `preparation.ts` proactive "DEFAULT OFF" comment (proactive is default-ON), `config-schema.ts` judgedCache "Default OFF" (judgedCache is default-ON). Gate every deletion on code (`!== false` reads), not comments.

**Commands (run from the worktree):** `bun install --frozen-lockfile`; `bun run check:fast` (lint+tsc+unit); `bun run check` (adds integration); `bun run lint`; `bunx tsc --noEmit`; `bun test <path> --timeout=30000`.

---

## 1. Binding decisions (made by this brief; grounded; reviewers hold the dev to these)

- **D1 (R29) — `getNeighborsByEntryId` is KEPT; only `getEntitiesByEntryIds` is deleted.** Plan §14.3's "both recombine-orphaned" premise is half wrong: `src/indexer/db/db.ts:896` `getNeighborsByEntryId`'s only production consumer is `consolidate.ts:59,:2859` (`narrowToIncrementalCandidates`, the `incrementalSince`/`--since` incremental-consolidation path, pinned by `tests/commands/consolidate/consolidate-incremental.test.ts`) — LIVE, PRESERVED consolidate behavior (R10). Deleting it would break the chunk's own consolidate-preservation gate. `getEntitiesByEntryIds` (`db.ts:1109`) is genuinely recombine-only (`recombine.ts:61,:644`) and is deleted. `getNeighborsByEntryId` enters the deletion ledger as retained-with-reason. (R29, R10)
- **D2 (plan §5 default-off branches) — all six default-off branches are deleted per the plan §5 DELETE row ("autoTune, dedup #617, judged-state cache #581, hotProbation, schemaSimilarity, proceduralAwareFloor"), but two are reclassified as LIVE behavior changes:** judged-state cache #581 is DEFAULT ON at HEAD (`consolidate.ts` opts `!== false`) and schemaSimilarity is DEFAULT ON since the reflect stabilization (`extract.ts` `!== false`, inert without derived embeddings). Their deletion changes default behavior (memories re-judged every consolidate run → more LLM calls; no schema-consistency penalty at extract). Both get §5 behavior-change-ledger entries and are reviewed as live changes (DoD 6) — not silently deleted as "dead". These branches carry no distinct inventory id; autoTune folds under R16, the rest are plan-§5 fold-ins.
- **D3 (R13/R16) — the self-confidence gate FRAMEWORK rides Chunk 6; Chunk 7 removes its improve-loop inputs + config, and the health calibration surface.** Plan §5 lists "self-confidence authorization" (R13) and "confidence/calibration auto-tuning" (R16) as "Deleted from loop AND config-schema", but plan §4.5 explicitly assigns `recordGateDecision` + `Proposal.gateDecision` + `runAutoAcceptGate` deletion to the proposals table = **Chunk 6** (which runs AFTER 7 in this wave). Binding split, respecting §4.5 and avoiding cross-chunk Proposal-type surgery: **Chunk 7 (WI-7.2)** deletes the exploration/self-confidence-driven INPUTS and their config knobs from the improve loop (exploration budget config + the loop plumbing per R14/R20); **Chunk 7 (WI-7.3)** deletes `calibration.ts` + `maybeAutoTuneThreshold` + the health calibration surface that consumes gate decisions (`health/types.ts`, `health/metrics.ts`, `health/improve-metrics.ts`, `health/html-report.ts`, `health.ts`, `health/windows.ts`) and the `calibration_autotune` event + write-side `persistPhaseThreshold` wiring; **Chunk 6** deletes the gate function `runAutoAcceptGate`, `recordGateDecision`, `Proposal.gateDecision`, the `autoAccept` config field, and Migration 012's `improve_gate_thresholds` read path (`makeGateConfig`/`getPhaseThreshold`). After Chunk 7 nothing WRITES gate decisions; the read path is left inert but intact for Chunk 6. R13's Chunk-7 acceptance is therefore "self-confidence inputs + config removed from the loop; nothing feeds the gate"; the gate's final removal is ledgered as a Chunk-6 continuation (top risk #1). Migration 012's CREATE TABLE is never edited (append-only).
- **D4 (R17) — the extract-side orderedActions/outcomeData capture is deleted with the procedural lane** (`extract-prompt.ts` capture blocks; `extract.ts` capture reads). Sole reader is `procedural.ts`. This changes default extract prompts/frontmatter → §5 ledger entry. Alternative (keep write-only frontmatter) rejected: violates the dead-code inventory.
- **D5 (R21 migration half; recombine table R28; judged-cache plan-§5) — one appended migration, historical entries untouched.** Migrations are append-only; entries 007/010/012/014 are never edited. WI-7.3 appends `018-drop-dead-lane-schema`: `DROP INDEX idx_recombine_hypotheses_*` + `DROP TABLE recombine_hypotheses` (R28); `DROP TABLE consolidation_judged` (judged-cache, plan-§5); `DROP INDEX idx_asset_outcome_review_pressure` + `ALTER TABLE asset_outcome DROP COLUMN review_pressure` (R21). The `tests/storage/sqlite-migrations.characterization.test.ts` DDL snapshot is re-recorded once, deliberately, in the same commit. Full `review_pressure` raw-SQL test census (repo-wide grep, complete — deletion gated by inventory): INSERT column lists in `tests/integration/mv-durable-recovery.test.ts:116,:166,:286`, `tests/integration/goldens-mv-recovery.test.ts:134`, `tests/commands/mv.test.ts:1229,:1236,:1243`, `tests/commands/improve/outcome-invariance.test.ts:49` (drop the column from the list — fixture bytes unchanged); `tests/commands/mv.test.ts:1278-1284` SELECTs and asserts review_pressure VALUES — this is the outcome-carry-on-rename contract and needs SEMANTIC rework, not a line delete: keep the `retrieval_count`/`outcome_score` carry assertions as the surviving pin, drop review_pressure from the SELECT, the row type, and expected objects; `tests/commands/improve/monitor-liveness.test.ts:35` and `tests/commands/improve/outcome-loop.test.ts` break at TSC on the FIELD deletion and are updated in WI-7.2. Migration-ID-pinning tests (`migration-backup.test.ts`, `migration-lifecycle-regression.test.ts`, `integration/migration-apply-crash.test.ts`, `storage/engine-cutover-historical-migrations.test.ts`) keep passing because no existing ID moves.
- **D6 (R6) — read-once seam with pass-scoped memoization + write-through invalidation; the hash-manifest defers to Chunk 6.** The plan's "hash-manifest snapshot" mechanism has ZERO hits in src at HEAD, and Chunk 6 (which runs AFTER 7) owns `beforeHash`/`FileChange[]`. Chunk 7 mints `RunContext.readAsset` — a read-once seam whose memo is **NEVER run-wide**. Scope rules, BINDING: (i) prep's six stage-body passes share ONE memo instance, created only AFTER the mutating pre-loop passes have returned (`runConsolidationPass`'s op writes are on disk before seeding) and discarded at stage end; (ii) reflect and distill each get a FRESH memo per verb invocation, so their reads still happen at invoke time — this is what "read timing unchanged" means, and it matters because mid-run writes are real: the loop's accepted proposals mutate files between verb invocations, and `distill.ts:816` stamps salience frontmatter onto the source asset in-loop; (iii) `RunContext` pairs `readAsset` with a write-invalidation hook (`noteAssetWrite(path)` or a `writeAsset` wrapper) that drops/refreshes the memo entry for a written path; every file-write site inside a live memo scope calls it. Consequence, pinned by unit test (WI-7.4): `readAsset` after an in-run write of the same path returns POST-write bytes, and reflect/distill reads observably happen at invoke time, not prep time. No content hashing is minted here.
- **D7 (R26) — FINISH the structured-call migration** (the plan's preferred branch, now strictly cheaper: 3 adopters at HEAD — `metadata-enhance.ts:63`, `graph-extract.ts:866`, `memory-infer-impl.ts:117` — and every chunk-7 raw caller already has a `chat` seam matching `CallStructuredRequest.chat`). Migrate exactly the chunk-7 files: `reflect.ts:904,:1483`, `distill.ts:740,:992` (WI-7.5); `extract.ts:1010,:1118` (WI-7.7); `consolidate.ts:1660,:3013`, `shared.ts:71`, `memory/memory-contradiction-detect.ts:222` (WI-7.8). Out-of-chunk raw callers (graph-extract retry path, tasks/runner, workflows, sources/schema-repair) are flagged for Chunk 9, left raw. `_setChatCompletionForTests` (`llm/client.ts`) must keep working through `structured-call.ts`'s late binding — verify with existing seam tests.
- **D8 (plan §5 mutateFrontmatter row) — partially descoped, recorded as mis-grounded.** The plan §5 CONSOLIDATE row "`memory-improve.ts:315-328,730-735` raw fs read/parse/write → `mutateFrontmatter`" is not cleanly executable: `:728-735` is a read-only collector (no write — nothing to convert); `:313-333` mutates the BODY (`resolveRelativeDates`), which `mutateFrontmatter`'s contract (frontmatter-only, content preserved) cannot express. Decision: leave both as-is; ledger records the ~−20 row as not executable as specced. Do NOT extend `core/asset/frontmatter.ts` (wide blast radius; the frozen `improve/resolve-relative-dates.json` golden pins block 1's bytes). Carries no inventory id.
- **D9 (R31) — the god-fn gate covers ALL of `src/commands/improve/**`,** including arrow/anonymous functions (the ~389-LOC anon in `loop-stages.ts`'s maintenance passes is real code), enforcement bar **220 lines** (= "~200" with tolerance). ~20 function-like nodes exceed it at HEAD, not the plan's 4-5. Mechanism: a TS-AST ratchet meta-test (WI-7.4) with a shrink-only exported baseline (pattern: `tests/lint-isolation-ratchet.test.ts` + `tests/architecture/agent-runner-seam.test.ts`), emptied by WI-7.8.
- **D10 (R7) — the envelope facade wraps the CURRENT Proposal shape** (`repository.ts:287` Proposal, `:198` `payload: {content, frontmatter?}`, status `pending|…`). No `FileChange[]`, no `beforeHash` — those land in Chunk 6. The facade is one module the five surviving emit sites adopt (`reflect.ts:1583`, `distill.ts:1231`, `distill/promote-memory.ts:248`, `extract.ts:825`, `consolidate.ts:2668`; the SC winner-persist `loop-stages.ts:337`, `recombine.ts:892`, `procedural.ts:453` die in WI-7.1/7.2/7.3).
- **D11 (R28/R17) — `'recombine'` and `'procedural'` are removed from `PROPOSAL_SOURCES`/`AUTOMATED_PROPOSAL_SOURCES`** (`proposal/repository.ts:101-102,:121-122`) and the recombine drain rule (`drain-policies.ts:56-61`) is deleted. Legacy pending proposals with those sources become unmatched and are swept by the existing 90-day TTL. Ledger note; no compatibility shim.
- **D12 (R19) — the four #616 stage seams are KEPT** (`improve.ts:131-137`: `collectEligibleRefsFn`/`runImprovePreparationStageFn`/`runImproveLoopStageFn`/`runImprovePostLoopStageFn`) — they are the DI that keeps `akmImprove` testable and WI-7.7 builds on them. Deleted: the cycle for-loop `:914-1135`, `maxCycles` option `:123-130` + resolution `:474` + config knob `config-schema.ts:653`, per-cycle accumulators, fixed-point break, inter-cycle git bank (#662 collapses to the single end-of-run commit), `runIndexAndCollect` re-run, `cyclesRun>1` reporting, multi-cycle comments. Default (`maxCycles=1`) single-pass behavior is byte-identical.
- **D13 (R18/R23) — behavior-change ledger entry (b) uses CORRECTED wording.** The plan's "P0-A is today the ONLY path for never-rated assets" is FALSE at HEAD: proactive-maintenance ships `enabled:true, maxPerRun:15` in the default strategy and the #608 high-salience lane also rescues never-rated refs (per-lane attribution pinned by `p0a-selection.json`: high-retrieval/high-salience/proactive/signal-delta). Post-deletion, never-rated assets remain reachable via proactive + high-salience; the delta is smaller than the plan claims and diff review must attribute per lane. Pull the exact ledger wording from `goldens-p0a-selection.test.ts`'s header (it already carries the correction).
- **D14 (R25) — no db handle into `ProposalsContext`/`createProposal` in this chunk.** `withProposalsDb` keeps its per-call open; threading the run handle risks the two-writers-on-one-WAL failure class (#585) and is explicitly Chunk 9 work. Chunk 7's R25 scope: the reflect/distill subtree (14 no-ctx `appendEvent` sites), `locks.ts:45`, and extract's 3 sites (`:765,:869,:1355`, currently passed a dbPath-only ProposalsContext for event emission).
- **D15 (R36) — `improve/self-consistency.json` and `improve/p0a-selection.json` are RETIRED with their lanes** (the designated path per their re-baseline@7 notes): suite + fixture + DESIGNATIONS.json entry removed in the same commit (WI-7.2). All consolidate-surface fixtures + `improve/signal-delta-gate.json` + the two duration-flag fixtures are frozen-migration-input — zero diffs allowed all chunk.
- **D16 (R20) — Chunk 7 deletes both config subtrees** (`ImproveCalibrationSchema` `config-schema.ts:817-836`, `ImproveExplorationSchema` `:839-853`, registrations `:906-907`): §5 binds the schema halves to the lane deletion and both sole readers die in this chunk. Verify the parent schema's passthrough leniency keeps existing user configs with orphaned `improve.calibration`/`improve.exploration` blocks loading. Note the overlap in the chunk report for Chunk 9's config pass.
- **D17 (R8) — three-verb naming is vocabulary ONLY**: revise = reflect; learn = extract/distill/inference (recombine/synthesis removed from "learn" by §13.1); consolidate = the existing memory-tier ops. It lands as module/pass naming, doc comments, and the facade's documentation. Zero new machinery, zero renamed CLI surface, zero config keys.

---

## 2. Ground truth (re-verified on the wave-1 worktree — these anchors win over the plan's)

God functions in `src/commands/improve` over the 220 bar (AST-measured): `runImprovePreparationStage` preparation.ts:838-2390 (1553), `akmImprove` improve.ts:401-1309 (909), `akmReflect` reflect.ts:948-1623 (676), `runImproveLoopStage` loop-stages.ts:78-721 (644), `akmDistill` distill.ts:680-1312 (633), `akmExtract` extract.ts:905-1383 (479), `runImproveMaintenancePasses` loop-stages.ts:939-1408 (470, contains the ~389-LOC anon at :1009), `planConsolidation` consolidate.ts:1387-1812 (426), `akmRecombine` recombine.ts:605 (393, deleted), `processSession` extract.ts:552-901 (~350, **20** positional args — `standardsContext` was added post-plan), `handleMergeOp` consolidate.ts:2117-2413 (297), `runConsolidationPass` preparation.ts:229-527 (~299), anon recombine.ts:706 (287, deleted), `promoteMemoryToKnowledge` distill/promote-memory.ts:73-326 (254), `narrowConsolidationPool` consolidate.ts:1125-1376 (252), `handlePromoteOp` consolidate.ts:2477-2690 (214), improve-cli.ts `run` :165-373 (209), `runImprovePostLoopStage` loop-stages.ts:723-930 (208), `sanitizeReflectPayload` reflect.ts:686-892 (~207), `runSessionExtractPass` preparation.ts:529-734 (~206).

Already landed upstream — do NOT re-plan/re-churn:
- The reflect double disk-read collapse (single read at `reflect.ts:1020-1038`). (R3)
- `akmConsolidateInner` is already the narrow→plan→apply 3-pass shape with 4 exported op handlers dispatched via `ConsolidateOpContext` (incl. the `generateMergedContentFn` seam). R9's remaining scope is decomposing `planConsolidation`/`handleMergeOp`/`handlePromoteOp` INTERNALLY, not creating that outer split. (R9)
- The `EventsContext.db` fast path (`events.ts:254-266`) + `akmImprove`'s long-lived handle (`improve.ts:838-846`): ~50 of ~65 improve-area appendEvent sites are ctx-adopted; `loop-stages.ts` (13) and `preparation.ts` (14) already thread it. Remaining hot no-ctx sites: reflect.ts:953,:980,:1403,:1491,:1605; distill.ts:716,:1035,:1112,:1249,:1280; distill/promote-memory.ts:159,:266,:296; distill/quality-gate.ts:310; plus locks.ts:45 and extract.ts:765,:869,:1355 (dbPath-only ctx). R25's real gap = exactly this subtree. (R25)
- The promotion-policy bench recompute (`tests/commands/distill/promotion-policy-corpus.ts` + `distill-promotion-policy.bench.test.ts`) — only the literal trim remains. `{selectedModel}` alone is `{name,threshold}` in the SELECTION result, but production's `DEFAULT_PROMOTION_POLICY` recovers the FULL 13-field `PromotionModelConfig` for the winning model via `CANDIDATE_MODELS.find(...)`; the trim target is a frozen SINGLE `PromotionModelConfig` (all 13 weight fields for the selected model) inlined in place of the ~50-line `CANDIDATE_MODELS` array + the ~840-line `DEFAULT_PROMOTION_POLICY_SELECTION` payload — NOT a 2-field literal, or `assessWithWeightedModel` breaks at runtime. (R24)
- Three prep pre-loop passes are already extracted (`runConsolidationPass`, `runSessionExtractPass`, `runValidationAndRepairPass`). (R1)

"LOOK/CHANGE separation" and "corrective-evidence" have NO code identifiers. Their code embodiments, for the preservation review: the signal-delta gate (`eligibility.ts:349-431` — `isSignalDeltaEligible` strict `fb > lastProposal`) + the `preparation.ts:1030-1153` partition (the 2026-05-26 "54-ref simultaneous-reflect incident" fix), and within consolidate the read-only narrow/plan vs mutating apply pass boundary (dry-run returns before `applyConsolidationPlan`). The gate does NOT live inside consolidate's op handlers — do not expect to find or preserve a LOOK/CHANGE construct there; the frozen `signal-delta-gate.json` golden is the pin, in `eligibility.ts`/`preparation.ts`. (R10)

`superseded_by` (snake_case archive frontmatter, `consolidate.ts:863`) ≠ `supersededBy` (camelCase belief edge, `memory-belief.ts:135`). The R10 preserve list means the former.

Salience keep-intact set: `computeSalience` weight switch, `upsertAssetSalience` #644 CASE non-lowering guard, `isContentEncodingRow`, `DEFAULT_TYPE_ENCODING_WEIGHTS`, `computeValenceScore`, `evaluateCollapseAlerts`, deterministic ranking. The three `!== false` outcome-weight reads (`distill.ts:751`, `salience.ts:356`, `preparation.ts:1747`) are moved VERBATIM, never flipped (§9.3 parity flip is deferred — out of scope). `updateAssetOutcome`'s only caller is `preparation.ts:1649`, reading only `result.outcomeScore` (:1659).

Chunk 0a oracles (all green at HEAD, all fixtures frozen `frozen-migration-input`): `tests/commands/consolidate/goldens-consolidate-ops.test.ts` (drives the four exported handlers directly; merge-1+1 primary and high-confidence contradict pinned byte-for-byte — header names itself "the Chunk 7 DoD 5 oracle"), `goldens-consolidate-journal.test.ts` (journal round-trip via real `akmConsolidate`, recovery matrix, all-hot early-exit with zero LLM calls, guard verdicts), `goldens-merge-plans.test.ts` (precedence byte-for-byte), `tests/commands/improve/goldens-signal-delta-gate.test.ts` (truth table + partition counts), `goldens-self-consistency.test.ts` + `goldens-p0a-selection.test.ts` (the SC/P0-A before-state baselines, retired-with-lanes in WI-7.2). (R10, R32, R36)

---

## 3. Work items (ordered; each ≈ one focused dev session)

### WI-7.1 — Delete the recombine/synthesis subsystem + the orphaned index-DB export
**testMode: deletion-gate** · dependsOn: — · est: ~−1,300 src / ~−2,750 test (reported under §12.1's residual row) · Reqs: R28, R29, R30 (this sweep's ledger + greps), R37 (this sweep's test pairing), R8 (recombine dropped from "learn")

Tests first (enumerate before touching src):
- DELETE: `tests/commands/improve-recombine.test.ts`, `tests/commands/improve-recombine-promote.test.ts`, `tests/recombine-tuning.test.ts`, `tests/recombine-drain-accept.test.ts`, `tests/state-db/recombine-hypotheses.test.ts`.
- UPDATE: `tests/graph-db-rekey.test.ts` (drop the `getEntitiesByEntryIds` contract block), `tests/_helpers/graph-store.ts` (comment), `tests/standards-prompt-injection.test.ts` (drop the `buildClusterPrompt` import + its recombine case + header comment — same commit as the recombine.ts deletion or the suite fails to resolve; the `buildProceduralPrompt` half stays until WI-7.3), `tests/commands/default-improve-strategies.test.ts` + its snapshot, `tests/commands/improve-strategy-selection.test.ts`, `tests/improve-strategies.test.ts` (recombine strategy fields only — its hotProbation cases are WI-7.3's), `tests/improve-shared-helpers.test.ts` (processKey), `tests/config-process-roundtrip.test.ts` (recombine fields), `tests/commands/improve/collapse-detector.test.ts` (pass union), `tests/health-checks-characterization.test.ts` (advisory wording).
- KEEP GREEN (oracles): `tests/commands/consolidate/consolidate-incremental.test.ts` (pins the SURVIVING `getNeighborsByEntryId` consumer), all four 0a consolidate golden suites (zero fixture diffs), `tests/storage/sqlite-migrations.characterization.test.ts` (unchanged in this item — schema drop is WI-7.3).

Steps:
1. Delete `src/commands/improve/recombine.ts` (1,009), `src/storage/repositories/recombine-repository.ts` (290), `src/assets/improve-strategies/synthesize.json`, `src/assets/improve-strategies/recombine-only.json`, `src/assets/prompts/recombine-system.md`.
2. Unwire: `loop-stages.ts` (pass invocation + `recombineWorked`/`pass` computation); `improve.ts` (`recombineFn` seam + `recombination` result field + reporting); `core/improve-types.ts` (`'recombine'` EligibilitySource literal, `RecombineResult`, `AkmImproveResult.recombination`); `core/improve-result.ts`; `core/events.ts` (`'recombine_invoked'`); `shared.ts` (processKey union → `'procedural'` only until WI-7.3); `proposal/repository.ts` + `drain-policies.ts` (per D11); `config-schema.ts:510-536,:605` (7 recombine-only fields + registration); `config-types.ts`; `engine-semantics.ts` (`recombine-only` const + engine map); `improve-strategies.ts` (recombine-only.json import/registration); `default.json` (`recombine:{enabled:false}`); `src/assets/help/help-improve.md`.
3. Rewire the KEPT collapse detector (mint/score math and stores stay byte-identical): the `loop-stages.ts` invocation becomes consolidate-only (pass always `'consolidate'`, acceptedActions no longer adds `lessonsPromoted`); narrow the WRITE-side pass unions at `collapse-detector.ts:270,:464` and `canaries-repository.ts:32` to `'consolidate'` while keeping READS tolerant of historical `'recombine'|'both'` row values; update advisory wording (`health/advisories.ts`) and comments (`health/types.ts`). (R37 — surviving-machinery rewire, not deletion)
4. Delete `getEntitiesByEntryIds` (`src/indexer/db/db.ts:1109`). Do NOT touch `getNeighborsByEntryId` (`db.ts:896`) — record retained-with-reason (D1).
5. Migration 014 stays in place verbatim (append-only); the table drop lands in WI-7.3's migration 018.
6. Grep-gate (zero hits in src/, scripts/, src/assets/; allowlist = migrations.ts 014 entry + WI-7.3's 018): `akmRecombine`, `RecombineResult`, `recombineFn`, `recombine_hypotheses`, `recombine_invoked`, `RecombineHypothesisRow`, `recombine-repository`, `deriveRecombineLessonRef`, `getEntitiesByEntryIds`, case-insensitive `recombin`. Do NOT gate on bare `synthesize` (legit uses in reflect prompts, #636, consolidate, health html) — use `synthesize.json` + the strategy-name entries instead.
7. Start `docs/design/execution/chunk-7/ledger.md`: inventory of deleted files/symbols, the retained-with-reason row for `getNeighborsByEntryId`, ledger entry for D11 (orphaned legacy proposal sources), recombine net-LOC (~−1,300 src, ledgered under §12.1 residual row).

Deletions: files `recombine.ts`, `recombine-repository.ts`, `synthesize.json`, `recombine-only.json`, `recombine-system.md`; symbols `getEntitiesByEntryIds`, `akmRecombine`, `RecombineResult`, `recombineFn`, `'recombine_invoked'`, the `'recombine'` PROPOSAL_SOURCES entries, the recombine drain rule, the recombine config subtree.

Acceptance:
- Grep-gate tokens above = 0 hits in the src/scripts/assets scope (allowlist honored). (R28, R29, R30)
- `consolidate-incremental.test.ts` green; 0a consolidate golden fixtures byte-identical; `collapse-detector.test.ts` green with consolidate-only wiring. (R10, R37)
- `bun run check:fast` green; no edits to fixed points. (R34)
- Ledger started with recombine inventory + `getNeighborsByEntryId` retained-with-reason. (R30)

### WI-7.2 — Delete the improve-loop lanes: self-consistency, P0-A, multi-cycle, exploration + self-confidence inputs, ValenceScore.lane, review_pressure (code/type)
**testMode: deletion-gate** · dependsOn: 7.1 · est: ~−800 src / ~−1,600 test · Reqs: R11, R13 (loop inputs + config half — gate framework is Chunk 6, D3), R14, R15, R18 (code/type; migration 018 is 7.3's), R19, R20 (exploration/maxCycles half), R22, R23 (ledger entries a+b), R30, R36 (SC/P0-A goldens retired), R37

Tests first:
- RETIRE with designations (same commit, per D15): `tests/commands/improve/goldens-self-consistency.test.ts` + `tests/fixtures/goldens/improve/self-consistency.json` + its DESIGNATIONS.json entry; `goldens-p0a-selection.test.ts` + `improve/p0a-selection.json` + entry. `tests/goldens-designations.test.ts` must stay green.
- DELETE: `tests/commands/improve/improve-multi-cycle.test.ts` (drives the deleted cycle loop; its AC1 "default maxCycles=1 ⇒ each stage seam called exactly once" is the byte-identical-default assertion — repurpose that one case into a surviving single-cycle regression test rather than losing the coverage).
- UPDATE (anchor by symbol/describe NAME, never by line range — this file's ranges are traps):
  - `tests/commands/improve/ws4-gate-coherence.test.ts` — remove exactly the five EXPLORATION describes (`"exploration budget"`, `"exploration budget exhaustion"`, `"no-confidence candidates and exploration"`, `"makeGateConfig exploration budget from config"`, `"exploration budget restoration on failure"`). Do NOT touch the auto-tune/calibration describes (`"auto-tune ceiling"`, `"per-phase auto-tune persistence"`, `"per-phase calibration isolation"`) — WI-7.3's; and the Chunk-6-owned `"per-phase threshold store (Migration 012)"` and `"makeGateConfig per-phase threshold resolution"` MUST stay green through the whole chunk.
  - `tests/commands/improve/outcome-loop.test.ts` — remove the `"updateAssetOutcome — review_pressure (#613)"` describe, the `.reviewPressure` result reads, the `reviewPressure` accumulator literal, and the `review_pressure: 0` row-literal fields; drop `review_pressure` from all its INSERT column lists (+ header comment) in the same edit (safe before the 018 column drop — DEFAULT 0 tolerates omission).
  - `tests/commands/improve/monitor-liveness.test.ts` — drop the `review_pressure: 0` field from the typed `AssetOutcomeRow` row builders (the type import keeps compiling after the field deletion). Surviving-machinery suite: UPDATE, never delete.
  - `tests/commands/health-minting-rollup.test.ts` — shrink the ratified-lane-set pin to `["high-salience","proactive","signal-delta"]` and retire/rework the high-retrieval byLane test. Decision: historical `lane='high-retrieval'` proposal rows no longer roll up as an enrichment lane — a live reporting behavior change; one ledger sentence (step 9).
  - `tests/distill.test.ts` — switch the `eligibilitySource` attribution round-trip to a surviving lane (`'high-salience'`); the attribution contract itself is preserved.
  - `minRetrievalCount` option census (the option dies with P0-A; every typed options literal passing it hits a tsc excess-property error — delete the option line at each site, mechanical): `improve-path-exists-guard.test.ts`, `improve-memory.test.ts`, `improve-memory-misc.test.ts`, `proactive-maintenance-flow.test.ts` (also simplify the "ensure P0-A is NOT the path" comments), `consolidate-min-pool-size.test.ts`, `extract-min-new-sessions.test.ts`, `improve-planned-filepath.test.ts`, `extract-strategy-gate.test.ts`, `improve-eligibility.test.ts` (folded into its P0-A rework), `goldens-signal-delta-gate.test.ts` — EXPLICIT CARVE-OUT: test-SOURCE edits to this frozen-designation suite are permitted and required (drop the option line; `minRetrievalCount: 0` already neutralizes the lane, and the fixture's own note anticipates surviving the P0-A deletion) while the FIXTURE bytes stay frozen byte-identical.
  - `tests/commands/improve/feedback-valence.test.ts` (exact-shape `toEqual` loses `lane`; the dedicated "lane routing at the strong threshold" test is deleted outright), `tests/commands/improve/engine-plan-boundaries.test.ts` (`selfConsistencyThreshold`), `tests/config-process-roundtrip.test.ts` (exploration/maxCycles fields), `tests/commands/improve/improve-sync-resilience.test.ts` (maxCycles refs), `tests/proposal-gate-decision.test.ts` (exploration-budget reason strings only — gate mechanics are Chunk 6's).
- KEEP GREEN: `goldens-signal-delta-gate.test.ts` (fixture byte-identical; suite source edited only per the census carve-out), `outcome-invariance.test.ts` (untouched here — #691 formula pins unchanged; reads only `outcomeScore`; its `:49` INSERT column list is updated in WI-7.3 with migration 018), `improve-no-hang`, `improve-budget-watchdog`, `improve-session-lifecycle`, `improve-sync`, `improve-dry-run-side-effects`, `improve-lock-invariants`, `improve-db-locking`, `proactive-maintenance-flow` (green after its census edit).

Steps:
1. **Self-consistency (R15)** — real extents, wider than the plan's: `loop-stages.ts:116-173` (SC_THRESHOLD/SC_N/jaccardSimilarity/pickMajorityVote) AND `:308-365` including the winner-persist tail `:335-359` (createProposal with `reflect-sc-` sourceRun + cooldown diversion). Also sweep the SC-only surface: `improve.ts:203,:208` (`selfConsistencyThreshold`/`selfConsistencyN` options), `:270,:1492` (utilityScores plumbing in `ImproveRunContext`), and reflect's draftMode support existing solely for SC (option doc, no-op-vote guard, synthetic-draft branch, the `draftMode` option). There is NO config-schema field for SC (options-only — the plan's "AND config-schema" is inapplicable here; note in ledger). Do NOT touch `utilityMap`'s other uses (health observability + eligibility/attention scoring in `preparation.ts`).
2. **P0-A (R18)** — `preparation.ts:1270-1336` (threshold const :1270, candidate union :1276-1282, selection gate :1327-1330 — this is CODE; the :1033-1040 the plan cites is an explanatory COMMENT), noFeedbackPool deferral routing, loop-set merge, `eligibilitySource='high-retrieval'` stamps (:1555,:2039), summary log; `improve.ts` `minRetrievalCount` option; `improve-cli.ts:201-202,:292` `--min-retrieval-count`; `'high-retrieval'` union member `core/improve-types.ts`, `health/types.ts` ENRICHMENT_LANES, `repository.ts` doc text. Do NOT delete `retrievalCounts`/`getRetrievalCounts` (shared with the Layer-2 proactive-maintenance selector) — only the `highRetrievalRefs` filter and its downstream merge points. Grep `requireFeedbackSignal` for non-P0-A consumers before touching it.
3. **Multi-cycle (R19)** — per D12: delete the loop `improve.ts:914-1135` (single pass remains), `maxCycles` option+resolution+config knob (`config-schema.ts:653`), accumulators, fixed-point break, #662 inter-cycle git bank, re-collect guard, `cyclesRun` reporting; KEEP the four stage seams `:131-137`.
4. **Exploration + self-confidence loop inputs (R14, R13 Chunk-7 half)** — SURGICAL, not the plan's stale :77-215 line-range (that range now interleaves Chunk-6-owned gate code). Delete only the exploration-specific slices in `improve-auto-accept.ts`: `explorationBudgetCount` field, budget init, `isExploration` branches + guard, `eligibilitySource` override, slot restore, makeGateConfig's exploration-budget-fraction computation; `loop-stages.ts` candidateCount arg; `'exploration'` member `core/improve-types.ts`. LEAVE for Chunk 6 (D3): `runAutoAcceptGate`, `recordGateDecision`, suppression, archive-on-validation-failure, `getPhaseThreshold` — plus `Proposal.gateDecision` and the `autoAccept` config field. The KEPT deterministic drain engine (`proposal/drain.ts`) is untouched. R13's Chunk-7 outcome: the self-confidence-driven exploration inputs + their config are gone; the gate no longer receives exploration budget; its framework removal is a documented Chunk-6 continuation.
5. **review_pressure (R21 code/type)** — surgical inside `outcome-loop.ts` (the plan's :127-305 range interleaves KEPT outcome_score EMA/clip): constants `:131,:137`, row field `:148`, result field `:197`, warm-start `:229`, compute/decay `:266-275`, upsert column writes `:281,:290,:301`, return `:305`, plus `review_pressure` in the two SELECT column lists (`getAssetOutcome`/`getAllAssetOutcomes`) and the doc-comment refs. The DB column itself survives until WI-7.3's migration 018 (DEFAULT 0 tolerates omission from INSERT/UPDATE).
6. **ValenceScore.lane (R22)** — `feedback-valence.ts` (STRONG_VALENCE_THRESHOLD, FeedbackLane type, `lane` field, lane-null return, compute/assign). Consumers verified reading only `.valence`/`.attention` (`preparation.ts:1647-1655,:1894`).
7. **Config (R20, exploration/maxCycles half)** — `ImproveExplorationSchema` `config-schema.ts:839-853` + registration `:907`; `maxCycles` `:653`; verify passthrough leniency for orphaned user-config blocks (D16).
8. **Behavior-change ledger (R23)** — entry **(a)**: SC default-ON deletion → 3×→1× reflect LLM calls on utility≥0.7 refs (intended, self-agreement is not verification); entry **(b)**: P0-A with D13's CORRECTED attribution wording (proactive + high-salience still rescue never-rated assets); note **(b2)**: with `'high-retrieval'` removed from ENRICHMENT_LANES, historical rows with that lane no longer classify as enrichment in health reporting (intended, small). Pull wording from the golden test headers.
9. Grep-gate additions (scope src/scripts/assets): `selfConsistencyThreshold`, `selfConsistencyN`, `pickMajorityVote`, `jaccardSimilarity` (scope src/commands/improve), `explorationBudgetCount`, `high-retrieval`, `minRetrievalCount`, `maxCycles` (scope improve), `reviewPressure`, `FeedbackLane`, `STRONG_VALENCE_THRESHOLD`. Do NOT gate on bare `exploration` alone if it collides with Chunk-6-scope gate code; scope to the deleted identifiers.

Deletions: symbols/consts `SC_THRESHOLD`, `SC_N`, `jaccardSimilarity`, `pickMajorityVote`, `selfConsistencyThreshold`, `selfConsistencyN`, `draftMode`; the P0-A `highRetrievalRefs` filter + `minRetrievalCount` option + `--min-retrieval-count` flag + `'high-retrieval'` literal; the cycle loop + `maxCycles`; exploration slices in `improve-auto-accept.ts`; `review_pressure`/`reviewPressure` field+result (column deferred to 018); `ValenceScore.lane`/`FeedbackLane`/`STRONG_VALENCE_THRESHOLD`; config `ImproveExplorationSchema` + `maxCycles` knob. Files retired: `improve/self-consistency.json`, `improve/p0a-selection.json` (+ suites + DESIGNATIONS entries).

Acceptance:
- Grep-gate tokens = 0 in src/scripts/assets (review_pressure column string permitted only in migrations until 7.3). (R11, R14, R15, R18, R19, R22, R30)
- `goldens-designations.test.ts` green with both entries removed; no other fixture touched. (R36)
- ws4 Chunk-6-owned describes green; ws4 auto-tune describes untouched (7.3's); `proposal/drain.ts` + `runAutoAcceptGate`/`recordGateDecision` untouched (Chunk-6, D3); `outcome-invariance` untouched and green. (R13, R19)
- tsc clean across the full `minRetrievalCount` census; `monitor-liveness`, `health-minting-rollup`, `distill.test.ts` updated per list; `signal-delta-gate.json` fixture byte-identical despite the suite-source edit. (R18, R21, R36)
- Ledger entries (a)+(b)+(b2) written with corrected P0-A wording. (R23)
- `bun run check:fast` green; safety suites green. (R34)

### WI-7.3 — Delete the opt-in/default-off branches: calibration+autotune, procedural, dedup #617, judged-cache #581, hotProbation, schemaSimilarity, proceduralAwareFloor; append migration 018
**testMode: deletion-gate** · dependsOn: 7.2 · est: ~−1,950 src / ~−2,000 test, +~60 (migration + content-hash module) · Reqs: R16, R17, R20 (calibration half), R21 (migration-018 half), R23 (ledger for default-ON deletions + extract capture), R30, R37; plan §5 default-off-branch fold-in (dedup/judged-cache/hotProbation/schemaSimilarity/proceduralAwareFloor)

Tests first:
- DELETE: `tests/improve-calibration.test.ts` (confirm `tests/akm-eval-judge-calibration.test.ts` is the UNRELATED akm-eval judge harness — it is; do NOT delete it), `tests/commands/improve-procedural.test.ts`, `tests/commands/improve/dedup.test.ts`, `tests/commands/improve/dedup-cache-wiring.test.ts`, `tests/commands/consolidate/consolidate-judged-cache.test.ts`, `tests/commands/improve/hot-probation.test.ts`, `tests/commands/improve/schema-similarity-gate.test.ts`, `tests/issue-641.test.ts` (pins the deleted `proceduralAwareFloor`).
- UPDATE: `ws4-gate-coherence.test.ts` — remove exactly the three AUTO-TUNE/CALIBRATION describes by NAME (`"auto-tune ceiling"`, `"per-phase auto-tune persistence"`, `"per-phase calibration isolation"`); the Chunk-6-owned describes stay green (exploration describes already removed in 7.2). `tests/health-md-report.test.ts` (summarizeCalibration import; judgedCacheSkipped fixture field), `tests/health-ws5-observability.test.ts` (judgedCacheSkipped counter), `tests/commands/improve/improve-target-integration.test.ts` (remove the `perfTelemetry?.judgedCacheSkipped` assertion — or re-point at a surviving field — and the `hotProbation` config blocks), `tests/improve-strategies.test.ts` (the hotProbation freeze/mutation cases; its recombine half was 7.1's), `tests/extract-command.test.ts` (incl. hotProbation plan cases), `tests/extract-watch.test.ts`, `tests/commands/improve/extract-triage-gate.test.ts` (keep default-path triage coverage), `tests/extract-prompt.test.ts` (capture fields, D4), `tests/standards-prompt-injection.test.ts` (`buildProceduralPrompt` import + its case; the buildClusterPrompt half was 7.1's), `tests/config-process-roundtrip.test.ts`, `tests/coverage-hardening/core-config-walker.test.ts`, `tests/storage/sqlite-migrations.characterization.test.ts` (snapshot re-recorded ONCE for 018), and the FULL migration-018 raw-SQL census per D5.
- KEEP GREEN: all four 0a consolidate golden suites with ZERO fixture diffs (judged-cache/dedup removal must not perturb them — ops suite drives handlers directly; journal scenarios never populate `consolidation_judged`), `consolidate-op-handlers`, `consolidate-all-hot-skip`, `consolidate-incremental`.

Steps:
1. **Calibration + autoTune (R16, D3)** — delete `src/commands/improve/calibration.ts` (294); `maybeAutoTuneThreshold` `preparation.ts:121-204` (docblock from :99) + call sites `preparation.ts:505,:2355`, `loop-stages.ts:700-716`; re-export `improve.ts:80` + stale comment block; `calibration_autotune` event (`events.ts`); `persistPhaseThreshold` WRITE-side wiring only. Health cascade per D3 (types/metrics/improve-metrics/html-report/health/windows — stub the calibration surface out or remove it; do NOT leave a dangling non-optional `calibration: CalibrationSummary`). Leave migration 012 + `makeGateConfig` phaseThreshold READS intact (Chunk 6).
2. **Procedural (R17, D4)** — delete `src/commands/improve/procedural.ts` (501) + `src/assets/prompts/procedural-system.md`; unwire `loop-stages.ts` (proceduralFn invocation); `improve.ts` (`proceduralFn` seam + `proceduralCompilation` result field + reporting); `improve-types.ts` (`ProceduralCompilationResult`, `AkmImproveResult.proceduralCompilation`); `improve-result.ts`; `events.ts` (`procedural_compiled`); `config-schema.ts:537-546,:606` (procedural-only fields + key); `config-types.ts`; `engine-semantics.ts` (procedural engine entry); `shared.ts` (processKey union dissolves); `PROPOSAL_SOURCES 'procedural'` (D11); `default.json`. Delete the extract-side capture per D4 (`extract-prompt.ts` capture blocks; `extract.ts` capture reads) with a ledger entry. DO NOT touch `proceduralAwareFloor` here — that is step 7 (different feature).
3. **dedup #617 (plan §5)** — extract `cacheHash` + `stripFrontmatterBody` into new `src/commands/improve/content-hash.ts` (~40 LOC; kept consumers in `consolidate.ts`/`distill.ts`); then delete `dedup.ts` (565) wholesale, the consolidate branch + opts, wiring `preparation.ts`, config `config-schema.ts:323-342`. (This is the memory dedup pre-pass — Chunk 6's proposal dedup/cooldown in `repository.ts` is NOT touched.)
4. **judged-state cache #581 (plan §5 — DEFAULT ON, D2)** — delete `consolidate.ts` opts + `computeMemoryContentHash` + narrowing + recording + the `judgedCacheSkipped` perfTelemetry field with its consumers (health surfaces + the three test files above); `src/storage/repositories/consolidation-repository.ts` (77); config `config-schema.ts:343-351`. Ledger entry: unchanged memories are re-judged each consolidate run (more LLM calls) — live change, plan's default-off framing stale (D2).
5. **hotProbation (plan §5)** — delete `src/commands/improve/hot-probation.ts` (50); its extract/consolidate wiring; config. SAFETY BOUNDARY: the `captureMode:'hot'` merge/delete guards (`consolidate.ts:2253,:2432-2443`) and indexer captureMode machinery (`ranking-contributors.ts`, `passes/metadata.ts`) protect user `akm remember` memories and MUST survive.
6. **schemaSimilarity (plan §5 — DEFAULT ON, D2)** — delete `src/commands/improve/schema-similarity-gate.ts` (196); its `extract.ts` wiring; the `schemaSimilarityCtx` processSession arg; config. Ledger entry: live intake-gate change where derived embeddings exist (D2).
7. **proceduralAwareFloor (plan §5)** — `triage.ts` (the #641 heuristic branches); `extract.ts` reads; config `config-schema.ts:552-556`. The default-path triage sub-scores (toolDensity/editCommit) are KEPT. This is a SEPARATE feature from `procedural.ts` — do NOT delete it by name-matching `procedural`.
8. **Config (R20, calibration half)** — `ImproveCalibrationSchema` `config-schema.ts:817-836` + registration `:906`.
9. **Migration 018 (R21 half, D5)** — append `018-drop-dead-lane-schema` exactly as specified in D5 (drop recombine_hypotheses table+indexes, consolidation_judged table, review_pressure index+column); re-record the characterization DDL snapshot once; update the FULL raw-SQL census from D5 (mv-durable-recovery, goldens-mv-recovery, mv.test.ts INSERTs + the `:1278-1284` semantic rework, outcome-invariance `:49`) in the same commit.
10. Ledger + grep-gate additions: `maybeAutoTuneThreshold`, `computeThresholdAutoTune`, `summarizeCalibration`, `gateDecisionsToSamples`, `CalibrationSummary`, `calibration_autotune`, `akmProcedural`, `ProceduralCompilationResult`, `procedural_compiled`, `buildProceduralPrompt`, `orderedActions`, `runDeterministicDedup`, `consolidation_judged` (allowlist: migrations 007 + 018), `computeMemoryContentHash`, `hotProbation`, `isHotProbation`, `schemaSimilarity`, `proceduralAwareFloor`, `review_pressure` (allowlist: migrations 010 + 018). Do NOT gate on bare `calibration` (akm-eval judge-calibration harness) or bare `procedural` (historical issue refs, `proceduralAwareFloor` was already deleted so this is only comment noise).

Deletions: files `calibration.ts`, `procedural.ts`, `procedural-system.md`, `dedup.ts`, `hot-probation.ts`, `schema-similarity-gate.ts`, `consolidation-repository.ts`; symbols per steps; config subtrees `ImproveCalibrationSchema`, dedup/judged-cache/hotProbation/schemaSimilarity/procedural fields, `proceduralAwareFloor`.

Acceptance:
- Grep-gate tokens = 0 in src/scripts/assets (allowlists honored); processSession arg count now 18 (20 at HEAD − `schemaSimilarityCtx` − `hotProbationEnabled`; `triage` stays as one arg with its `proceduralAwareFloor` field removed inside it). (R16, R17, R30)
- 0a golden fixtures byte-identical; `consolidate-all-hot-skip`/`consolidate-incremental` green; captureMode:'hot' guards + indexer captureMode machinery untouched. (R10, R34)
- Migration 018 appended, no historical entry edited; migration-ID-pinning suites green; characterization snapshot re-recorded exactly once. (R21, D5)
- Health calibration surface deleted/stubbed with no dangling non-optional `CalibrationSummary`; `akm-eval-judge-calibration.test.ts` untouched. (R16, D3)
- Ledger entries for #581, schemaSimilarity, extract-capture written as live behavior changes. (R23)
- `bun run check` (incl. affected integration) green. (R34)

### WI-7.4 — Mint the shared helpers: RunContext, proposal-envelope facade, serializeFrontmatterQuoted, single derived-ref impl, god-fn ratchet; DRY fixes + rename
**testMode: test-first** · dependsOn: 7.3 · est: +~550 new / −~120 dup; net ≈ +430 (the §5 ~+500 budget) · Reqs: R6 (mint readAsset seam, D6), R7 (mint envelope facade), R8 (vocabulary home), R12 (single derived-ref impl), R27 (serializeFrontmatterQuoted), R31 (mint the ratchet gate); plan §5 fold-ins: `MAX_REJECTED_PROPOSALS` dedup, `improve-result-file` rename, the mutateFrontmatter row (D8, descoped)

Tests first (WRITE these before implementation):
- `tests/core/asset-serialize.test.ts` — `serializeFrontmatterQuoted` contract: every scalar JSON.stringify-quoted; arrays serialize as `["a", "b"]` (distill's form — the canonical pick; recombine's unquoted-string variant died in 7.1 so no divergence remains); key order preserved; round-trips through `parseFrontmatter`. (R27)
- NEW `tests/commands/improve/derived-ref.test.ts` — producer/consumer agreement: same parent resolved for (i) `source:` needing parseAssetRef normalization (whitespace/origin), (ii) `derivedFrom:`-keyed memories, (iii) `.derived` name suffix; PINS the intended widening: `derivedFrom`-keyed families now receive contradiction edges from `detectAndWriteContradictions` (plan §6's "producer/consumer cannot disagree"). (R12)
- NEW `tests/commands/improve/proposal-envelope.test.ts` — facade emits through `createProposal` with arg-for-arg equivalence to a direct call (current shape: `payload.content` + optional frontmatter, status pending; NO beforeHash/FileChange fields exist). (R7)
- NEW `tests/commands/improve/run-context.test.ts` — the D6 memo contract, pinned: (i) scope — a memo created for one verb invocation/pass region never serves a later invocation (reflect/distill get a fresh memo per invoke; reads observably happen at invoke time, not prep time); (ii) write-through invalidation — `readAsset` after an in-run write of the same path (via `noteAssetWrite`/`writeAsset`) returns POST-write bytes; (iii) the prep-region memo is seeded only after the mutating pre-loop passes return. (R6)
- NEW `tests/architecture/improve-fn-size-ratchet.test.ts` — TS-AST scan (typescript devDep; precedent `tests/architecture/agent-runner-seam.test.ts`) of `src/commands/improve/**`: every function-like node (incl. arrows/anons) ≤220 lines unless in the exported shrink-only baseline (pattern: `tests/lint-isolation-ratchet.test.ts`); baseline = the current post-deletion offender list with recorded sizes; equality-asserted so it can only shrink. (R31)
- UPDATE `tests/commands/improve-result-to-file.test.ts` for the rename.

Steps:
1. `src/commands/improve/run-context.ts` — `RunContext` carrying: `config`, `eventsCtx: EventsContext` (do NOT change EventsContext's shape — `loop-stages.ts:434,:440,:625,:627` dereference `eventsCtx.db` directly), `proposalsCtx`, `chat`/`getLlmConfig`, `sourceRun`, `dryRun`, `signal`, `now`, and the D6 read seam: `readAsset(pathOrRef)` with a memo that is pass-/invocation-scoped (never run-wide) plus the `noteAssetWrite(path)`/`writeAsset` write-through invalidation hook — see D6 for the binding scope rules. No db handle into ProposalsContext (D14). Document the three-verb vocabulary here + in the facade (D17). (R6, R8)
2. `src/commands/improve/proposal-envelope.ts` — `emitProposal(ctx, input)` facade per D10; wraps `createProposal` with the CURRENT shape. Adoption happens in WI-7.5/7.7/7.8 (five sites). (R7)
3. `serializeFrontmatterQuoted` in `src/core/asset/asset-serialize.ts` (module doc already names this case); adopt at `distill.ts:492-497` and `content-repair.ts:94-96,:135-137,:158-160`. Note: content-repair array values (tags) change bytes from `["a","b"]` to `["a", "b"]` — verify no fingerprint golden embeds repaired-array frontmatter (grounding says none pins it). (R27)
4. Single keyed-on-ref `resolveParentRef`/`isDerivedMemory` (new `src/commands/improve/memory/derived-ref.ts`): semantics = `fm.source` via `parseMemoryRef` normalization → `fm.derivedFrom` via `makeAssetRef` → strip `.derived` suffix. Consumer (`memory-improve.ts:736,:739,:816-832`) adopts directly; producer (`memory-contradiction-detect.ts:127-154,:255-256`) converts filePath→ref via its existing `toMemoryRef` then calls the shared impl. Producer-side widening is intended and pinned by the new test. Note: no dedicated contradiction-detect test suite exists — coverage lands in the new `derived-ref.test.ts` + expanded assertions in `tests/commands/improve-memory-misc.test.ts`/`tests/belief-state-phase1a.test.ts`. (R12)
5. Single `MAX_REJECTED_PROPOSALS = 3` (export from an improve shared module); adopt at `reflect.ts:231/:285` and `distill.ts:908/:911`. (plan §5 CONSOLIDATE row)
6. mutateFrontmatter row per D8: no code change; ledger records it as mis-grounded (block :728-735 read-only; block :313-333 body-mutating, outside mutateFrontmatter's contract; `resolve-relative-dates.json` frozen golden pins its bytes). (plan §5, descoped)
7. Rename `relativeImproveResultPath` → `improveRunLocator`, `writeImproveResultFile` → `recordImproveRunResult` (matching sibling `recordTerminatedImproveRun`); update `improve-cli.ts` (stderr warning keeps its semantics) and the test file. File rename of `improve-result-file.ts` itself is optional; if renamed, keep it mechanical. (plan §5 RENAME row)

Deletions: duplicate serializer bodies (folded onto `serializeFrontmatterQuoted`), the two divergent `resolveParentRef`/`isDerivedMemory` impls (→ one), the two `MAX_REJECTED_PROPOSALS` declarations (→ one).

Acceptance:
- All five tests-first suites green; helpers exist with exactly one implementation each (`serializeFrontmatterQuoted` grep > 0, duplicate serializer bodies = 0; `resolveParentRef` defined once). (R7, R12, R27)
- `run-context.test.ts` pins the D6 memo contract: invoke-time reads (fresh memo per verb invocation) AND post-write bytes after in-run writes of the same path. (R6)
- Ratchet meta-test green with the recorded baseline; baseline file reviewed as the WI-7.5–7.8 worklist. (R31)
- Net add within the ~+500 §5 budget (reported). (R35)
- No change to `EventsContext` shape, `core/asset/frontmatter.ts`, or ProposalsContext. (D8, D14, R25)

### WI-7.5 — Decompose the revise (reflect) + learn (distill) verbs; trim the promotion-policy literal; adopt structured-call + events ctx in the reflect/distill subtree
**testMode: characterization-preserve** · dependsOn: 7.4 · est: ~1,300 LOC reorganized; net ≈ −900 (incl. −820 literal) · Reqs: R3, R4, R24, R25 (14 subtree sites), R26 (reflect/distill files), R31 (progress), R7 (facade adopt), R8 (naming), R6 (readAsset adopt)

Tests first:
- RESHAPE BEFORE TRIMMING (R24): `tests/commands/distill/distill-promotion-policy.bench.test.ts` — replace the full deep-equal against the frozen constant with (a) recompute via `selectPromotionPolicy(DEFAULT_PROMOTION_POLICY_CORPUS)` and (b) equality on `selectedModel.{name,threshold}` against the trimmed constant; `tests/commands/distill/distill-promotion-policy.test.ts` — trainingSize/heldOutSize/heldOut-metrics/baselines assertions switch from `getDefaultPromotionPolicySelection()` to live recompute; the `.selectedModel.name` assertion survives.
- KEEP GREEN (the characterization oracle): `tests/commands/reflect/reflect-size-gate.test.ts`, `reflect-noise-gate`, `reflect-propose`, `reflect-unattended-pin`, `reflect-completed-on-failure`, `reflect-pipeline-fixes`, `tests/commands/reflect-response-schema.test.ts`, `tests/commands/improve-reflect-unsupported-type-skip.test.ts`, `tests/issue-636-reflect-synthesize-description.test.ts`; `tests/distill.test.ts` (as updated in 7.2 for lane attribution; incl. Reflexion injection, assessment), `tests/commands/distill/promote-memory.test.ts`, `distill-content-repair`, `distill-response-schema`, `distill-cli-flag`, `tests/commands/improve/quality-gate-fail-closed.test.ts`, `distill-guards`, `tests/commands/improve-distill-planner-skip-lessons.test.ts`; `tests/llm/structured-call.test.ts` + adopter suites.

Steps:
1. **akmReflect (R3)** → thin orchestrator over named passes: `source-snapshot-read` (the existing single read `:1020-1038`, routed through `RunContext.readAsset`; index-lookup fallback + `options.assetContent` seam preserved; do NOT re-plan the landed double-read collapse), `revise-propose` (LLM call → sanitize → `emitProposal` facade at the `:1583` site), `size-gate` (the `checkReflectSize` call-site inside a slimmed `sanitizeReflectPayload` — the pure check stays in `proposal/validators/proposal-quality-validators.ts`; splitting the size/noise gates out takes `sanitizeReflectPayload` under the bar). draftMode surface is already gone (7.2).
2. **akmDistill (R4)** → orchestrator over: `extract-candidates` (the plan's name has no literal counterpart — DEFINED HERE as the input-resolution + refusal/type-guard region `:710-:764` incl. the single input read at `:764` via `readAsset`; the in-loop salience frontmatter stamp at `:816` writes that same file and MUST route through `writeAsset`/`noteAssetWrite` so the invocation-scoped memo serves post-write bytes — D6), `promote-policy-apply` (the `:869-900` memory→knowledge branch delegating to `promoteMemoryToKnowledge`, reading ONLY the frozen `{selectedModel}`), `distill-propose` (rejected-context `:908-915` + LLM + `emitProposal` at `:1231`). Decompose `promoteMemoryToKnowledge` (254) under the bar. The `outcomeWeightEnabled` read at `distill.ts:751` moves verbatim and remains the single read-site feeding quality-gate/promote-memory threading (no §9.3 flip).
3. **Promotion-policy trim (R24)** — `DEFAULT_PROMOTION_POLICY_SELECTION` (`distill-promotion-policy.ts:650-1491`, 842 lines) + the `CANDIDATE_MODELS` array (~50 lines) shrink to a frozen SINGLE `PromotionModelConfig` (all 13 weight fields for the selected model) with a narrow type; `SELECTED_MODEL`/`DEFAULT_PROMOTION_POLICY` derivations keep working against it; `getDefaultPromotionPolicySelection` (test-only callers) is removed. The bench recompute infra already exists — do NOT re-extract the corpus. (Correction to the plan's shorthand "frozen `{selectedModel}`": `{name,threshold}` alone is insufficient for `assessWithWeightedModel` — freeze the full model config.)
4. **Events ctx (R25)** — thread `RunContext`/`EventsContext` into the 14 no-ctx appendEvent sites: `reflect.ts:953,:980,:1403,:1491,:1605`; `distill.ts:716,:1035,:1112,:1249,:1280`; `distill/promote-memory.ts:159,:266,:296`; `distill/quality-gate.ts:310`.
5. **Structured-call (R26, D7)** — migrate `reflect.ts:904,:1483` and `distill.ts:740,:992` onto `callStructured`; existing `chat` option seams keep working (their shape matches `CallStructuredRequest.chat`); `_setChatCompletionForTests` behavior verified.
6. Naming: reflect passes live under the revise vocabulary, distill under learn (module docs/pass names only — D17). Ratchet baseline shrinks: `akmReflect`, `akmDistill`, `sanitizeReflectPayload`, `promoteMemoryToKnowledge` removed.

Acceptance:
- Every keep-green suite passes unchanged (fixtures untouched); reshaped policy tests green BEFORE the trim commit. (R3, R4, R24)
- `DEFAULT_PROMOTION_POLICY_SELECTION` literal ≤ ~15 lines (one full model config); production reads only the selected model. (R24)
- Zero raw `chatCompletion` calls remain in reflect.ts/distill.ts (grep); zero no-ctx `appendEvent(` in the reflect/distill subtree. (R25, R26)
- Ratchet baseline shrunk by the four functions; no new fn >220. (R31)
- Outcome-weight reads semantically unchanged (`!== false` at the moved sites). (R10)

### WI-7.6 — Decompose runImprovePreparationStage into the six named passes on RunContext
**testMode: characterization-preserve** · dependsOn: 7.4 (order after 7.5) · est: ~1,400 LOC reorganized; net ≈ −600 · Reqs: R1, R6 (adopt), R31 (progress); salience/outcome preservation (plan §11 DoD 6)

Tests first (all KEEP GREEN — this item adds no behavior):
- `tests/commands/improve/goldens-signal-delta-gate.test.ts` (FROZEN — partition counts + truth table byte-identical), `improve-eligibility.test.ts`, `salience-wiring.test.ts`, `outcome-invariance.test.ts` (#691 formula pins), `outcome-loop.test.ts` (post-7.2 shape), `improve-triage-prepass.test.ts`, `proactive-maintenance-flow.test.ts`, `consolidate-min-pool-size.test.ts`, `engine-plan-boundaries.test.ts`, `encoding-salience.test.ts`, `collapse-detector.test.ts`, `anti-collapse.test.ts`, `tests/ranking-salience-boost.test.ts`, `feedback-valence.test.ts`.

Steps (carve the 1,553-line stage body at `preparation.ts:838-2390` into six named pass functions on `RunContext`; the three already-extracted pre-loop passes stay):
1. `snapshot-manifest` — timestamp-map build (:1055-1079) + creation of the PREP-REGION `readAsset` memo, seeded only AFTER the three mutating pre-loop passes have returned (`runConsolidationPass`'s op writes are already on disk); frontmatter re-reads during validation/ranking route through it. The memo is discarded at stage end and is NEVER handed to reflect/distill (D6 scope rules).
2. `candidate-gather` — the signal-delta partition into eligibleRefs/distillOnlyRefs/noFeedbackPool/fullySkipped (:1087-1265; the 2026-05-26 fix comment and partition semantics move VERBATIM), proactive-maintenance selector (:1338-1386), high-salience/#608 + forgetting-safety rescue. P0-A is already gone (7.2).
3. `salience-score` — outcome scoring loop (:1622-1663, `updateAssetOutcome` caller reading only `.outcomeScore`) + `computeSalience`/`upsertAssetSalience` wiring (:1745-1810). Machinery moved verbatim — the `:1747` `!== false` read stays the single read-site; #644 guard, `isContentEncodingRow`, weights, tables untouched.
4. `valence-score` — the `computeValenceScore` call sites (:1647-1655 `.valence`, :1894 `.attention`); `feedback-valence.ts` machinery untouched (minus `.lane`, gone in 7.2).
5. `standards-context` — locate and extract the standards-context assembly currently inline in the stage (threaded onward to processSession/reflect).
6. `eligibility-filter` — replay budget (:2074) + deterministic ranking + summary emit (:2212-2390).
7. `runImprovePreparationStage` becomes a thin orchestrator (<220) sequencing the six passes; trim `runConsolidationPass` (~299) and `runSessionExtractPass` (~206) under the bar. Ratchet baseline shrinks accordingly.

Acceptance:
- All keep-green suites pass; `signal-delta-gate.json` fixture byte-identical. (R1, R10)
- Six pass functions exist with the plan's names; orchestrator <220; ratchet baseline shrunk by `runImprovePreparationStage`, `runConsolidationPass`, `runSessionExtractPass`. (R1, R31)
- Salience/outcome/valence machinery diff is move-only (reviewer checks the three `!== false` reads and the #644 guard verbatim). (R10, plan §11 DoD 6)
- No new file reads outside `readAsset` in the six passes (grep `readFileSync` in the pass modules); the prep-region memo is created post-pre-loop-passes, discarded at stage end, and never escapes into the verb invocations (D6 scope rules — `run-context.test.ts` pins the contract). (R6)

### WI-7.7 — Decompose akmImprove + loop-stages + akmExtract; RunContext into processSession; land the appendEvent hot-path test
**testMode: characterization-preserve** (the R25 hot-path test is test-first) · dependsOn: 7.5, 7.6 · est: ~1,600 LOC reorganized; net ≈ −450 · Reqs: R2, R5, R25 (remainder + hot-path test), R26 (extract), R31 (progress)

Tests first:
- NEW (WRITE FIRST, R25): `tests/commands/improve/appendevent-hot-path.test.ts` — asserts improve hot paths never hit appendEvent's slow (double-open) path (the ~170-redundant-opens-per-cycle target). Sanctioned techniques (mock.module is lint-banned; sandbox/_preload frozen): (a) handle-identity DI per `improve-db-locking.test.ts` — inject `eventsCtx: {db}` and assert every stage/verb callee received the identical handle; (b) counting-proxy `Database` as `EventsContext.db` — drive `akmReflect`/`akmDistill`/stage fns with a RunContext carrying the proxy and assert all event rows land in the injected handle (slow path would open new connections). Prefer DI over adding a production counter seam; if a seam proves unavoidable, flag it to reviewers against §1.3.
- KEEP GREEN: `improve-no-hang`, `improve-budget-watchdog`, `improve-session-lifecycle`, `improve-sync`, `improve-sync-resilience`, `improve-dry-run-side-effects`, `improve-lock-invariants`, `improve-db-locking`, `tests/extract-command.test.ts`, `tests/extract-session-tracking.test.ts` (#602 skip lives INSIDE processSession), `extract-min-new-sessions`, `extract-strategy-gate`, `extract-triage-gate`, `tests/session-indexing.test.ts`, `tests/extract-prompt.test.ts`, `tests/extract-watch.test.ts`.

Steps:
1. **akmImprove (R2)** — cycle loop already deleted (7.2); extract the remaining god-fn mass into named units: run-setup (lock acquire `locks.ts` path, index+collect closure, long-lived `eventsDb` open `:838-846`), stage sequencing via the KEPT #616 seams (D12), run-teardown (sync/error/catch-path plumbing, result recording via the renamed `recordImproveRunResult`). Orchestrator <220. Build the internal args on `RunContext`; `ImproveRunContext` either narrows onto it or is absorbed — without changing `EventsContext` shape.
2. **processSession (R5)** — signature `extract.ts:552-593` collapses from 18 positional args (20 at HEAD − schemaSimilarityCtx − hotProbationEnabled, both removed in 7.3) to `(runCtx: RunContext, session: SessionInput)`; SessionInput = per-session values `{sessionRef, prior, force}`; run-scoped values (harness, stashDir, config, getLlmConfig, chat, proposalsCtx, sourceRun, dryRun, timeoutMs, maxTotalChars, minContentChars, triage, sessionIndexing, signal, standardsContext) ride RunContext. Sole caller `extract.ts:1225` updated. RE-COUNT the args immediately before implementation (was 20 at HEAD; the split must not silently drop a field). Decompose `akmExtract` (905-1383): candidate discovery, per-session loop, summary — each <220.
3. **loop-stages** — `runImproveLoopStage` (post-deletion ~490) → per-ref pass sequence (reflect→distill invocation via existing `reflectFn`/`distillFn` seams, budget/watchdog, outcome events); `runImprovePostLoopStage` (208) trimmed; `runImproveMaintenancePasses` (470, incl. the ~389-LOC anon at :1009) → named maintenance passes (memory cleanup, contradiction detect, collapse detector, retention purges — the pre-existing `purgeOld*` retention calls are NOT lifecycle machinery; keep verbatim). `improve-cli.ts` `run` (209) trimmed.
4. **Events ctx remainder (R25)** — `locks.ts:45` and extract's 3 sites (`:765,:869,:1355`) take the run EventsContext (extract's dbPath-only ProposalsContext-as-ctx replaced for event emission; per D14 proposal WRITES keep their current path).
5. **Structured-call (R26)** — migrate `extract.ts:1010,:1118` onto `callStructured`.
6. Ratchet baseline shrinks: `akmImprove`, `akmExtract`, `processSession`, `runImproveLoopStage`, `runImprovePostLoopStage`, `runImproveMaintenancePasses`, the loop-stages anon, improve-cli `run`.

Acceptance:
- R25 hot-path test green and meaningful (fails if a subtree site reverts to no-ctx); the ~170-opens-per-cycle claim is exercised. (R25)
- processSession = 2 params; all keep-green suites pass. (R5, R2)
- Zero no-ctx `appendEvent(` sites under src/commands/improve (grep); zero raw `chatCompletion` in extract.ts. (R25, R26)
- Ratchet baseline shrunk; only consolidate-family functions remain. (R31)
- `eventsCtx.db` deref sites still compile against unchanged EventsContext. (R25)

### WI-7.8 — Decompose the consolidate ops into named passes; finish structured-call; final gates, ledger, grep-gate, net-LOC report
**testMode: characterization-preserve** · dependsOn: 7.7 · est: ~950 LOC reorganized; net ≈ −300 · Reqs: R9, R10, R26 (consolidate/shared/memory files), R31 (baseline emptied), R32 (goldens replay), R33 (audit), R11 (audit), R34 (final), R35 (net-LOC), R30 (final grep-gate), R23 (ledger finalize), R36 (consolidate replay), R7/R8 (facade + naming audit), R37 (audit)

Tests first (the byte-for-byte oracle — ALL KEEP GREEN with ZERO fixture diffs):
- `tests/commands/consolidate/goldens-consolidate-ops.test.ts` (imports `handleMergeOp`/`handleDeleteOp`/`handlePromoteOp`/`handleContradictOp`, `ConsolidateOpContext` incl. `generateMergedContentFn` seam, op types from `src/commands/improve/consolidate`, `MemoryEntry` from `consolidate/types` — this export surface MUST stay resolvable; re-exports fine, import-path updates in the suite permitted, fixture bytes frozen), `goldens-consolidate-journal.test.ts` (journal helpers are module-private, observed via `spyOn(fs.writeFileSync/copyFileSync)` — if decomposition moves them, interception points may need mechanical updates; fixtures are the oracle), `goldens-merge-plans.test.ts`, `goldens-signal-delta-gate.test.ts`; plus `consolidate-op-handlers`, `consolidate-chunks`, `consolidate-pipeline-fixes`, `consolidate-writer-fixes`, `consolidate-all-hot-skip`, `consolidate-incremental`, `consolidate-eligibility`, `consolidate-promote-dedup`, `tests/commands/goldens-duration-flags.test.ts` (parseSinceToIso at `consolidate.ts:2824` is Chunk 9 scope — UNTOUCHED).

Steps:
1. **planConsolidation (:1387-1812, 426) (R9)** → named passes mirroring the existing narrow/plan/apply shape: llm-config-resolve, chunk-sizing, similarity-clustering, anti-collapse-injection, chunking, pending-hash-preload, cold-start-budget, per-chunk-judge loop (:1581-1766 — the abort-rate policy, all-hot early-exit, and the 2026-05-26 accounting invariant `processed == actioned + judgedNoAction + Σ skipReasons + failedChunkMemories` incl. per-secondary skip emission and failed/aborted-chunk bucketing move VERBATIM; counter increment points must not move), merge-plans reconcile. Judged-cache recording is already gone (7.3).
2. **handleMergeOp (:2117-2413, 297) (R9)** → passes: skip-accounting closure, pre-flight existence/hot guard, content-generate, validate, post-generation hot re-check (KEEP BOTH hot checks; they differ behaviorally — pre-flight avoids the LLM call and changes the skip reason; post-gen catches participants turning hot mid-run — and the golden refusal matrix pins the pre-flight path — do NOT dedupe), provenance/anti-collapse, write/archive/journal/accounting (`archiveMemory` :838 semantics incl. `superseded_by` byte-identical).
3. **handlePromoteOp (:2477-2690, 214) (R9)** → the gate chain as named gate helpers in EXACT current order (within-run dedup :2492, pending-by-target :2518, destination-exists :2526, sanitization :2546, superseded refusal :2557, <100-char floor :2575, body-hash dedup :2596, frontmatter validation :2624, slug-variant pre-emit :2656) ending in the `emitProposal` facade at the `:2668` site. `handleDeleteOp`/`handleContradictOp` are already small — untouched beyond the facade/ctx.
4. Trim `narrowConsolidationPool` (:1125-1376, 252) under the bar; `getNeighborsByEntryId` consumption at :2859 unchanged (D1).
5. **Structured-call (R26)** — migrate `consolidate.ts:1660,:3013`, `shared.ts:71`, `memory/memory-contradiction-detect.ts:222`.
6. Facade adoption completes (consolidate promote site); vocabulary audit (D17): consolidate passes named under the consolidate verb; revise/learn naming from 7.5 checked; no machinery added. (R7, R8)
7. **Final chunk gates**:
   - Empty the ratchet baseline; flip the meta-test to the absolute assertion (no fn >220 in `src/commands/improve/**`, no allowlist). (R31)
   - Run the full grep-gate (7.1/7.2/7.3 token lists) over src/scripts/assets and record zero-counts in the ledger. (R30)
   - R33/R11 audit: scoped grep for new states/water-marks/pressure computation/intake blocking/CAS archive/sandbox gate/purge-quarantine COMMANDS — pre-existing retention `purgeOld*` calls, `memory-belief.ts` beliefState convention, and `schema-similarity-gate` history are allowlisted vocabulary; `review_pressure` count must be ZERO (deleted), not merely flat. No new labeling/clamps/prompts/digests/trust records (the standalone consolidate HTTP confirm at :1998-2024 is pre-existing, preserved, not new). (R11, R33)
   - Finalize `docs/design/execution/chunk-7/ledger.md`: deletion inventory per item; behavior-change ledger — (a) SC 3×→1× (intended), (b) P0-A with D13 corrected attribution, (c) recombine deletion per §13.1, (d) judged-cache #581 default-ON, (e) schemaSimilarity default-ON, (f) procedural extract-capture, (g) PROPOSAL_SOURCES orphaning, (h) mutateFrontmatter row mis-grounded (D8), (i) `getNeighborsByEntryId` retained-with-reason, (j) the R13 self-confidence-gate Chunk-6 continuation; net-LOC REPORTED (~−3,900 chunk area + ~−1,300 residual fold-in) — never a pass/fail threshold. (R23, R30, R35)
   - Full `bun run check` green (typecheck + unit + affected integration, serial invocations, `--timeout=30000` on any direct `bun test`); safety suites green; `git diff` empty on the four fixed-point paths. (R34)
   - Confirm no frozen fixture changed anywhere in the chunk (`git diff --stat tests/fixtures/goldens` shows only the two retired fixtures + DESIGNATIONS.json from 7.2 and the single 018 characterization-snapshot re-record from 7.3). (R32, R36)

Acceptance:
- All 0a golden suites green, fixtures byte-identical (merge/delete/promote/contradict outcomes, journal round-trip, hot-capture guard, contradiction preserve-and-qualify — byte-for-byte where deterministic). (R9, R10, R32, R36)
- No fn >220 in improve; baseline empty. (R31, DoD 5)
- R33/R11 audits recorded clean; ledger finalized and committed; grep-gate zero-counts recorded; net-LOC reported. (R11, R23, R30, R33, R35)
- `parseSinceToIso` and its frozen golden untouched; export surface for the ops suite intact. (R34)
- Canary probe/canary_queries/encoding-salience/outcome-loop(minus review_pressure) untouched beyond 7.1's sanctioned rewiring. (R37)

---

## 4. Gate checklist (manifest gates → items)

| Gate | Satisfied by |
|---|---|
| §5 behavior-change ledger attached to the chunk | Entries written in 7.2 (SC, P0-A-corrected) and 7.3 (#581, schemaSimilarity, extract-capture); D11/D8/D3 notes in 7.1/7.2/7.4; finalized + committed in 7.8 (R23, R30, R35) |
| Consolidate behavior preservation vs Chunk 0a goldens (§12.3, DoD 5) | Frozen fixtures byte-identical through 7.1 (collapse rewire), 7.3 (judged-cache/dedup removal), 7.6 (signal-delta), 7.8 (op decomposition + replay) (R10, R32, R36, R34) |
| No lifecycle state model (§6, §12.4) | Standing prohibition on every item; scoped audit in 7.8; review_pressure grep → 0 (R11, R33) |
| No god fn >~200 LOC remains in improve (DoD 5) | Ratchet minted 7.4 (220-line bar, whole directory incl. anons); shrunk 7.5/7.6/7.7; emptied + flipped absolute in 7.8 (R31) |

## 5. Requirements coverage (R1–R37 → items)

R1→7.6 · R2→7.7 · R3→7.5 · R4→7.5 · R5→7.7 · R6→7.4 (mint readAsset seam, D6) + adopt 7.5/7.6 · R7→7.4 (mint) + adopt 7.5/7.7/7.8 · R8→7.4 (vocab home) + 7.5 + 7.8 · R9→7.8 · R10→7.8 (+ kept green 7.1/7.3/7.6) · R11→all items, audited 7.8 · R12→7.4 · R13→7.2 (loop inputs + config; gate framework = Chunk 6 per plan §4.5, D3) · R14→7.2 · R15→7.2 · R16→7.3 · R17→7.3 · R18→7.2 (code/type) · R19→7.2 · R20→7.2 (exploration/maxCycles) + 7.3 (calibration/procedural) · R21→7.2 (code/type) + 7.3 (migration 018 + raw-SQL census) · R22→7.2 · R23→7.2 + 7.3 (entries) + 7.8 (finalize) · R24→7.5 · R25→7.5 (subtree) + 7.7 (remainder + hot-path test) · R26→7.5 + 7.7 + 7.8 · R27→7.4 · R28→7.1 · R29→7.1 · R30→7.1 + 7.2 + 7.3 (per-item) + 7.8 (final) · R31→7.4 (mint) + 7.5/7.6/7.7 (satisfy) + 7.8 (empty/flip) · R32→7.8 (+ frozen-green throughout) · R33→all items, audited 7.8 · R34→every item, final 7.8 · R35→7.8 (finalize) + per-item report · R36→7.2 (SC/P0-A goldens retired) + 7.8 (consolidate replay) · R37→7.1 + 7.2 + 7.3 (per-chunk test pairing), audited 7.8

Plan-§5 deliverables with no distinct inventory id (grounded, not invented): default-off branches dedup #617 / judged-cache #581 / hotProbation / schemaSimilarity / proceduralAwareFloor → 7.3; `MAX_REJECTED_PROPOSALS` dedup + `improve-result-file` rename + mutateFrontmatter row (D8, descoped) → 7.4.

## 6. Top risks

1. **R13 self-confidence gate spans two chunks.** Plan §5 says "delete from loop AND config-schema" (Chunk 7); plan §4.5 assigns `runAutoAcceptGate`/`recordGateDecision`/`Proposal.gateDecision` deletion to the proposals table (Chunk 6, runs after 7). D3 splits it: Chunk 7 removes the improve-loop self-confidence/exploration INPUTS + their config so nothing feeds the gate; Chunk 6 removes the framework. R13's Chunk-7 acceptance is scoped accordingly, and the continuation is ledgered. A reviewer applying §5 literally must be pointed at §4.5 + the wave order.
2. **`getNeighborsByEntryId` deletion would break preserved behavior** — D1 resolves it (keep; ledger retained-with-reason). Any reviewer applying the manifest's §14.3 text literally must be pointed at `consolidate.ts:2859` (the live `--since` incremental-consolidation consumer, pinned by `consolidate-incremental.test.ts`). Only `getEntitiesByEntryIds` is safe to delete.
3. **Two "default-off" deletions are live changes** (#581 judged-cache, schemaSimilarity — both `!== false` at HEAD, i.e. DEFAULT ON). D2: proceed with ledger entries; reviewers sign off via DoD 6, not silently.
4. **Line-range deletions are traps**: exploration (:77-215 interleaves Chunk-6 gate code), review_pressure (:127-305 interleaves kept EMA + two SELECT lists), SC (plan ranges miss the winner-persist tail + reflect draftMode), P0-A (the :1033-1040 the plan cites is a COMMENT; the code is :1270-1336 threaded through ~10 sites to :2039), ws4-gate-coherence (three owners share one file — anchor by describe NAME: exploration=7.2, auto-tune/calibration=7.3, migration-012/threshold=Chunk 6 keep-green). Every deletion in 7.2/7.3 is symbol- or describe-name-anchored, not range-anchored.
5. **Frozen-fixture discipline**: only two fixtures may leave the tree (retired with lanes, DESIGNATIONS.json same commit); every other golden byte-identical; `AKM_UPDATE_GOLDENS` never set except the single characterization-snapshot re-record in 7.3. Frozen designation binds the FIXTURE bytes, not the suite source: mechanical test-source edits to `goldens-signal-delta-gate.test.ts` (minRetrievalCount census) and `goldens-consolidate-journal.test.ts` (spy interception points) are permitted and sometimes required.
6. **Option/type deletions have a wide tsc blast radius**: deleting `minRetrievalCount` alone touches ~12 test files (census in 7.2); `AssetOutcomeRow.review_pressure`, `judgedCacheSkipped`, and `'high-retrieval'`/ENRICHMENT_LANES each break typed literals in suites the plan never names. The census lists in 7.1/7.2/7.3 are the inventory — run `bunx tsc --noEmit` immediately after each symbol deletion; a hit outside the census means the inventory was incomplete (stop and extend it, don't ad-hoc fix).
7. **D6 memo staleness is the silent failure mode**: a run-wide readAsset memo would serve prep-time bytes after mid-run writes (`distill.ts:816`, accepted-proposal applies, consolidate op writes) and no golden pins write-then-reread. The scope rules + write-through invalidation in D6 are binding; `run-context.test.ts` is the pin.
8. **`{selectedModel}` trim shorthand**: `{name,threshold}` alone breaks `assessWithWeightedModel` (needs the full 13-field model config). Freeze the single selected `PromotionModelConfig`, not a 2-field literal (R24, WI-7.5 step 3).
9. **`proceduralAwareFloor` ≠ `procedural.ts`**: the #641 triage-gate heuristic (`config-schema.ts:552-556`, `triage.ts`, `extract.ts`) is a different feature — do NOT name-match `procedural` when deleting `procedural.ts` (WI-7.3 steps 2 vs 7).
10. **Append-only migrations**: 007/010/012/014 never edited; one appended 018; characterization snapshot re-recorded exactly once. No DROP-COLUMN precedent exists — 018 is the first; verify it applies cleanly on an already-migrated state.db in the migration-apply integration suite.
11. **Test invocation**: `--timeout=30000` mandatory; serial runs only.
12. **Grep false positives**: never gate on bare `synthesize`/`calibration`/`procedural`/`pressure`/`exploration`; use the precise token lists in 7.1/7.2/7.3. Bare `calibration` collides with the unrelated akm-eval judge-calibration harness; bare `procedural` with `proceduralAwareFloor` and historical issue comments.
13. **Scope discipline (out by design)**: no lifecycle/trust machinery, no §9.3 outcome-weight parity flip, no `parseSinceToIso`/Chunk-9 touches, no ProposalsContext db handle (D14), no Chunk-6 gate-framework surgery (D3).
