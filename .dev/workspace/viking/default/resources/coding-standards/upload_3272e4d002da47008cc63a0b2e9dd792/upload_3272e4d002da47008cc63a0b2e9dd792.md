# Svelte `$effect` Guidance

- Do not use the `$effect` rune unless there are no other clean options.
- Prefer derived state, event handlers, load functions, or explicit state transitions first.
- Reach for `$effect` only when the reactive side effect is genuinely the clearest implementation.
