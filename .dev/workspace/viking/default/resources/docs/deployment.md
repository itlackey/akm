# Deployment Guide

To deploy OpenPalm, you need Docker and Docker Compose installed. The setup wizard walks you through initial configuration including API keys, model selection, and channel setup.

Security is LAN-first by default. Nothing is publicly exposed without explicit user opt-in. The admin container is the sole orchestrator with Docker socket access.
