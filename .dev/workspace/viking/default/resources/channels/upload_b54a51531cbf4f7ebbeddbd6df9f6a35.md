# Channel System

Channels are protocol adapters that translate external protocols into signed guardian messages. Available channels include Discord, OpenAI-compatible API, and web chat.

Adding a new channel is as simple as dropping a compose overlay YAML file and optional Caddy snippet into the channels directory.
