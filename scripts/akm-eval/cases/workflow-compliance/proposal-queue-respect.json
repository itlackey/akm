{
  "schemaVersion": 1,
  "id": "proposal-queue-respect",
  "suite": "workflow-compliance",
  "type": "workflow-compliance",
  "description": "Verifies proposal-queue respect per current-stash proposal lifecycle. In-scope `promoted` events must reference a proposal that entered the queue before promotion; unrelated events from other stashes, outside the window, or without a current proposal lifecycle are ignored. Skips cleanly when no current-stash proposal lifecycle exists in the window.",
  "input": {},
  "expected": {
    "proposalQueueRespect": true
  },
  "scoring": {
    "deterministic": true,
    "passThreshold": 0.8
  },
  "requires": {
    "minAkmVersion": "0.8.0"
  },
  "tags": ["workflow", "compliance", "proposals"]
}
